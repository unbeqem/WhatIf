---
phase: 02-stripe-webhook-pro-unlock
plan: 04
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - vitest.config.ts
  - lib/stripe.test.ts
  - package.json
autonomous: true
requirements: [PAY-03, PAY-05]

must_haves:
  truths:
    - "vitest is installed and `npm test` runs the suite in the terminal in seconds."
    - "planFromSubscription maps unit_amount 500 → 'pro', 900 → 'creator'."
    - "planForEvent returns the paid tier for active/trialing subscriptions, keeps the paid tier when cancel_at_period_end=true + active, and returns 'free' for canceled/unpaid/deleted."
    - "The reducer is proven WITHOUT any network, Stripe SDK call, or DB access (pure-function tests)."
  artifacts:
    - path: "vitest.config.ts"
      provides: "vitest test runner config"
      contains: "defineConfig"
    - path: "lib/stripe.test.ts"
      provides: "unit tests for planFromSubscription + planForEvent"
      contains: "planForEvent"
    - path: "package.json"
      provides: "test script + vitest devDependency"
      contains: "vitest"
  key_links:
    - from: "lib/stripe.test.ts"
      to: "lib/stripe.ts planForEvent / planFromSubscription"
      via: "import + assert"
      pattern: "from \"(\\./stripe|@/lib/stripe)\""
---

<objective>
Introduce vitest (locked founder decision) and unit-test the pure event→plan reducer from Plan 02-01: `planFromSubscription` (amount→tier) and `planForEvent` (event+status→tier). These functions carry the two highest-risk pieces of logic (tier mapping and cancel-at-period-end entitlement, PAY-03/PAY-05) and are pure, so they test with zero mocking of HTTP/DB.

Purpose: The manual Stripe-CLI walkthrough (Plans 02/03) remains the E2E gate; this gives fast, deterministic regression coverage on the logic most likely to silently break — satisfying the Nyquist automated-verification requirement for the reducer.

Output: `vitest.config.ts`, `lib/stripe.test.ts`, `package.json` test script + vitest devDependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-stripe-webhook-pro-unlock/02-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- From Plan 02-01 (lib/stripe.ts) — the functions under test. -->
From lib/stripe.ts:
  export function planFromSubscription(sub: Stripe.Subscription): "pro" | "creator";
    // reads sub.items.data[0]?.price.unit_amount; >=900 → "creator", else "pro"
  export function planForEvent(eventType: string, sub: Stripe.Subscription): PlanTier | null;
    // "customer.subscription.deleted" → "free"
    // "checkout.session.completed"|"customer.subscription.updated":
    //    status active|trialing → planFromSubscription(sub); else → "free"
    // other event types → null
  export type PlanTier = "free" | "pro" | "creator";

package.json today has NO test script and NO test framework (devDeps: tailwind/postcss/types/typescript only).
Node/Next 16 project; TypeScript 5.7. vitest runs .ts directly (esbuild) — no extra transform config needed
for pure-logic tests (no React/JSX in these tests).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Install vitest + config + test script (RED harness)</name>
  <read_first>
    - package.json (add devDependency + script)
    - lib/stripe.ts (confirm planForEvent/planFromSubscription exported — from Plan 02-01)
    - .planning/phases/02-stripe-webhook-pro-unlock/02-RESEARCH.md (Validation Architecture → Option A)
  </read_first>
  <action>
    1. Install: `npm i -D vitest` (dev-only; no @vitejs/plugin-react needed — these are pure-logic tests, no JSX).
    2. Create `vitest.config.ts`:
       ```typescript
       import { defineConfig } from "vitest/config";
       export default defineConfig({
         test: {
           include: ["lib/**/*.test.ts"],
           environment: "node",
         },
       });
       ```
    3. In `package.json` scripts add: `"test": "vitest run"` and `"test:watch": "vitest"`.
    Do not change existing scripts (dev/build/start/lint).
  </action>
  <verify>
    <automated>npx vitest run --reporter=basic || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"vitest"' package.json` >= 1 (devDependency present)
    - `grep -c '"test": "vitest run"' package.json` == 1
    - File `vitest.config.ts` exists and `grep -c "defineConfig" vitest.config.ts` == 1
    - `npx vitest run` executes (exit code may be non-zero only if no tests yet — the runner itself must launch without a config error)
  </acceptance_criteria>
  <done>vitest installed, config includes lib/**/*.test.ts on node env, npm test wired. Runner launches.</done>
</task>

<task type="tdd">
  <name>Task 2: Reducer unit tests (RED → GREEN against Plan 02-01 functions)</name>
  <read_first>
    - lib/stripe.ts (exact behavior of planFromSubscription + planForEvent)
    - vitest.config.ts (include glob)
    - .planning/phases/02-stripe-webhook-pro-unlock/02-RESEARCH.md (Pattern 3 cancel_at_period_end, Mapping price → plan tier, Phase Requirements → Test Map)
  </read_first>
  <behavior>
    planFromSubscription:
      - unit_amount 500 → "pro"
      - unit_amount 900 → "creator"
      - unit_amount 1200 → "creator" (>=900)
      - missing items/price (unit_amount undefined) → "pro" (0 < 900 fallback)
    planForEvent:
      - ("checkout.session.completed", status "active", amount 500) → "pro"
      - ("checkout.session.completed", status "active", amount 900) → "creator"
      - ("customer.subscription.updated", status "active", cancel_at_period_end true, amount 500) → "pro" (STILL Pro — Pattern 3)
      - ("customer.subscription.updated", status "trialing", amount 900) → "creator"
      - ("customer.subscription.updated", status "canceled") → "free"
      - ("customer.subscription.updated", status "unpaid") → "free"
      - ("customer.subscription.deleted", any status) → "free"
      - ("invoice.paid", anything) → null (unhandled type ignored)
  </behavior>
  <action>
    Create `lib/stripe.test.ts`. Import `{ planFromSubscription, planForEvent }` from `"./stripe"`.
    Build a minimal typed subscription factory to avoid depending on the full Stripe.Subscription shape:
    ```typescript
    import { describe, it, expect } from "vitest";
    import { planFromSubscription, planForEvent } from "./stripe";
    import type Stripe from "stripe";

    function sub(opts: {
      amount?: number;
      status?: Stripe.Subscription.Status;
      cancelAtPeriodEnd?: boolean;
    }): Stripe.Subscription {
      return {
        status: opts.status ?? "active",
        cancel_at_period_end: opts.cancelAtPeriodEnd ?? false,
        items: {
          data: opts.amount === undefined
            ? []
            : [{ price: { unit_amount: opts.amount } }],
        },
      } as unknown as Stripe.Subscription;
    }
    ```
    Write the assertions covering EVERY case in <behavior>. Use `describe`/`it`/`expect`.
    These are pure calls — assert NO network/DB is touched (there is nothing to mock; the test importing
    lib/stripe.ts must not trigger any Stripe or Supabase call at module load — if it does, that's a
    Plan 02-01 defect to flag, not to mock around).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/stripe.test.ts` exists.
    - `grep -c "planForEvent" lib/stripe.test.ts` >= 6 (all planForEvent cases)
    - `grep -c "planFromSubscription" lib/stripe.test.ts` >= 3
    - `grep -c "cancel_at_period_end\|cancelAtPeriodEnd" lib/stripe.test.ts` >= 1 (Pattern 3 covered)
    - `npm test` exits 0 (all assertions pass)
    - No network/DB import in the test file: `grep -c "supabaseAdmin\|fetch(\|http" lib/stripe.test.ts` == 0
  </acceptance_criteria>
  <done>lib/stripe.test.ts covers amount→tier, active/trialing entitlement, cancel_at_period_end-stays-Pro, canceled/unpaid/deleted→free, and unhandled→null; `npm test` green with no mocking of HTTP/DB.</done>
</task>

</tasks>

<verification>
- `npm test` exits 0 with the full reducer suite passing.
- Suite runs in seconds (pure functions, node env).
- No Stripe SDK network call or Supabase access occurs during the test run.
</verification>

<success_criteria>
- vitest installed + wired via `npm test`.
- planFromSubscription + planForEvent proven across tier mapping, status-based entitlement, cancel-at-period-end, and unhandled events — no network, no DB, no mocks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stripe-webhook-pro-unlock/02-04-SUMMARY.md`.
</output>
