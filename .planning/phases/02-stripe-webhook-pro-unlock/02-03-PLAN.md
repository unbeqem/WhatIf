---
phase: 02-stripe-webhook-pro-unlock
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/api/stripe/portal/route.ts
  - app/account/page.tsx
  - app/account/ManageSubscriptionButton.tsx
  - components/AuthNav.tsx
autonomous: false
requirements: [PAY-06]
user_setup:
  - service: stripe
    why: "Customer Portal sessions cannot be created in test mode until portal settings are saved once in the Stripe test dashboard"
    dashboard_config:
      - task: "Open Settings → Billing → Customer portal and click Save (once) in TEST mode"
        location: "Stripe Dashboard (test mode) → Settings → Billing → Customer portal"

must_haves:
  truths:
    - "A logged-in user has an /account page showing their email and current plan (free/pro/creator)."
    - "A subscribed user (has stripe_customer_id) can click Manage subscription and land on the Stripe Customer Portal."
    - "A free user on /account sees an unlock CTA instead of a portal button."
    - "AuthNav links a logged-in user to /account."
    - "The portal route derives the customer id from the caller's OWN profile row (never the request body); no id / demo mode → redirect to pricing or a demo notice, not a 500."
  artifacts:
    - path: "app/api/stripe/portal/route.ts"
      provides: "authenticated portal-session creation from the caller's stripe_customer_id"
      contains: "createPortalSession"
    - path: "app/account/page.tsx"
      provides: "account surface: email, plan, Manage subscription / unlock CTA"
      contains: "createSupabaseServerClient"
    - path: "app/account/ManageSubscriptionButton.tsx"
      provides: "client button that posts to /api/stripe/portal and redirects"
      contains: "use client"
    - path: "components/AuthNav.tsx"
      provides: "link to /account for logged-in users"
      contains: "/account"
  key_links:
    - from: "app/account/ManageSubscriptionButton.tsx"
      to: "/api/stripe/portal"
      via: "Manage subscription button posts to the portal route"
      pattern: "/api/stripe/portal"
    - from: "app/api/stripe/portal/route.ts"
      to: "profiles.stripe_customer_id"
      via: "reads the authenticated user's own customer id"
      pattern: "stripe_customer_id"
    - from: "app/api/stripe/portal/route.ts"
      to: "createPortalSession"
      via: "server-side portal session"
      pattern: "createPortalSession\\("
---

<objective>
Give subscribers a self-serve billing home (PAY-06): a `/account` page showing plan + email, a Manage subscription button that opens the Stripe Customer Portal, and an unlock CTA for free users. Add the portal route and an AuthNav link.

Purpose: Locked founder decision — `/account` is the home for PAY-06. Subscribers manage/cancel via Stripe's Portal (PCI + cancel/invoice UI stays with Stripe); free users get the upgrade path.

Output: `app/api/stripe/portal/route.ts`, `app/account/page.tsx`, `app/account/ManageSubscriptionButton.tsx`, an `/account` link in `components/AuthNav.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-stripe-webhook-pro-unlock/02-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- From Plan 02-01 (lib/stripe.ts) — import, do not re-implement. -->
From lib/stripe.ts:
  export const stripe: Stripe | null;
  export async function createPortalSession(customerId: string): Promise<string | null>; // null in demo
  export type Plan = "pro" | "creator";

Auth (server): lib/supabase/server.ts → createSupabaseServerClient(); await supabase.auth.getUser()
Admin (server, service-role): lib/supabase/admin.ts → supabaseAdmin (or null in demo)
  profiles Row: { id, plan: "free"|"pro"|"creator", stripe_customer_id: string|null, stripe_subscription_id, created_at, updated_at }

Existing components to reuse on /account:
  components/UpgradeButton.tsx  → <UpgradeButton plan="pro"|"creator">…</UpgradeButton> (posts to /api/stripe)
  components/AuthNav.tsx        → client component; shows email + logout for logged-in users.
    Logged-in block currently renders: a <span> with the email pill + a logout <button>.
    Insert a <Link href="/account"> in that logged-in block (styled like the existing email pill).

Conventions (CLAUDE.md): server components by default; "use client" only where interactive;
  API routes validate + return JSON; Tailwind tokens (bg-surface, border-border-hi, text-fg-soft,
  from-violet to-magenta) already in globals.css — reuse, don't add tokens.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portal route — authenticated Customer Portal session (PAY-06)</name>
  <read_first>
    - lib/stripe.ts (createPortalSession, stripe — from Plan 02-01)
    - lib/supabase/server.ts (createSupabaseServerClient)
    - lib/supabase/admin.ts (supabaseAdmin)
    - app/api/simulate/route.ts (auth + runtime conventions)
    - .planning/phases/02-stripe-webhook-pro-unlock/02-RESEARCH.md (Customer Portal section, Pitfall 5, Security Domain: IDOR row)
  </read_first>
  <action>
    Create `app/api/stripe/portal/route.ts`:
    ```typescript
    import { NextResponse } from "next/server";
    import { stripe, createPortalSession } from "@/lib/stripe";
    import { createSupabaseServerClient } from "@/lib/supabase/server";
    import { supabaseAdmin } from "@/lib/supabase/admin";

    export const runtime = "nodejs";

    export async function POST() {
      const baseUrl = process.env.NEXT_PUBLIC_URL ?? "http://localhost:3000";

      // Demo-mode invariant: no Stripe → do not 500; send them home with a notice flag.
      if (!stripe || !supabaseAdmin || !process.env.NEXT_PUBLIC_SUPABASE_URL) {
        return NextResponse.json({ demo: true, url: `${baseUrl}/account?portal=demo` });
      }

      const supabase = await createSupabaseServerClient();
      const { data: auth } = await supabase.auth.getUser();
      const userId = auth?.user?.id;
      if (!userId) {
        return NextResponse.json({ error: "not_authenticated" }, { status: 401 });
      }

      // IDOR mitigation: customer id comes from the CALLER'S OWN row, never the request.
      const { data: profile } = await supabaseAdmin
        .from("profiles")
        .select("stripe_customer_id")
        .eq("id", userId)
        .maybeSingle();

      const customerId = profile?.stripe_customer_id ?? null;
      if (!customerId) {
        // Never checked out — nothing to manage. Point at pricing.
        return NextResponse.json({ url: `${baseUrl}/#pricing`, noCustomer: true });
      }

      const url = await createPortalSession(customerId);
      return NextResponse.json({ url: url ?? `${baseUrl}/account` });
    }
    ```
    Do NOT accept a customerId (or userId) from the request body — this is the IDOR control (T-02-08).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `app/api/stripe/portal/route.ts` exists.
    - `grep -c "createPortalSession(" app/api/stripe/portal/route.ts` == 1
    - `grep -c "auth.getUser()" app/api/stripe/portal/route.ts` == 1
    - customerId read from own profile: `grep -c '.eq("id", userId)' app/api/stripe/portal/route.ts` == 1
    - No customerId sourced from request: `grep -c "req.json\|request.json\|body.customer" app/api/stripe/portal/route.ts` == 0
    - Demo-mode preserved: `grep -c "demo: true" app/api/stripe/portal/route.ts` == 1 guarded by `if (!stripe`
    - Unauthenticated → 401: `grep -c "not_authenticated" app/api/stripe/portal/route.ts` == 1
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>Portal route authenticates the caller, reads their own stripe_customer_id, opens a Stripe portal session (or redirects to pricing when no customer / demo notice when keyless), never trusts a client-supplied id. Build green.</done>
</task>

<task type="auto">
  <name>Task 2: /account page + colocated Manage-subscription client button + AuthNav link</name>
  <read_first>
    - components/AuthNav.tsx (logged-in block to add the /account link into)
    - components/UpgradeButton.tsx (reuse for the free-user CTA; reference client-component + fetch pattern)
    - lib/supabase/server.ts + lib/supabase/admin.ts (read plan server-side)
    - app/result/page.tsx OR app/decision/page.tsx (Tailwind token usage + page shell pattern)
  </read_first>
  <action>
    1. Create the colocated client button `app/account/ManageSubscriptionButton.tsx` (the FIRST line MUST be `"use client"`):
       - A minimal client component that on click does `fetch("/api/stripe/portal", { method: "POST" })`, reads `data.url` from the JSON, then `window.location.href = data.url`.
       - Style it like the primary CTA (reuse Tailwind tokens: from-violet to-magenta, rounded-xl, px-5 py-3, text-sm font-semibold text-white). An optional loading state ("Opening…") is fine.
       - This MUST be its own file — it CANNOT be folded into `app/account/page.tsx`: that page is a SERVER component that awaits `createSupabaseServerClient()` and imports the `server-only` `supabaseAdmin`. Adding `"use client"` to that module would turn the whole file into a client module and illegally import server-only code (tsc/runtime error). Keep the interactive button in this separate client file and import it into the server page.
    2. Create `app/account/page.tsx` as a SERVER component (default — no "use client"):
       - `import { createSupabaseServerClient } from "@/lib/supabase/server";` and `supabaseAdmin`.
       - `import ManageSubscriptionButton from "./ManageSubscriptionButton";`
       - `const { data: auth } = await (await createSupabaseServerClient()).auth.getUser();`
       - If no user → `redirect("/login")` (import from "next/navigation").
       - Read plan + stripe_customer_id from profiles via supabaseAdmin `.eq("id", user.id).maybeSingle()` (fall back to plan "free" if null).
       - Render (reuse existing Tailwind tokens — bg-surface, border-border-hi, text-fg-soft/text-fg, from-violet to-magenta):
         - The account email.
         - Current plan label: Free / Pro / Creator.
         - If plan is pro OR creator (or stripe_customer_id present): render `<ManageSubscriptionButton />` (the client component from step 1).
         - If plan is free: render `<UpgradeButton plan="pro">Unlock Pro — €5/mo</UpgradeButton>` and `<UpgradeButton plan="creator">Go Creator — €9/mo</UpgradeButton>`.
       - Copy is English (CLAUDE.md).
    3. Edit `components/AuthNav.tsx`: in the logged-in branch (where the email pill + logout button render), add a `<Link href="/account">` styled like the email pill (reuse the existing pill classes: rounded-full border border-border-hi bg-surface/60 px-4 py-2 text-sm). Place it before the logout button. Keep the component's existing hydration + demo guards (`if (!isSupabaseConfigured) return null`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `app/account/ManageSubscriptionButton.tsx` exists.
    - `grep -c '"use client"' app/account/ManageSubscriptionButton.tsx` == 1 (its own client module)
    - `grep -c "/api/stripe/portal" app/account/ManageSubscriptionButton.tsx` == 1 (Manage subscription button target)
    - File `app/account/page.tsx` exists and is NOT a client module: `grep -c '"use client"' app/account/page.tsx` == 0
    - `grep -c "createSupabaseServerClient" app/account/page.tsx` == 1
    - `grep -c "ManageSubscriptionButton" app/account/page.tsx` >= 1 (imported + rendered)
    - `grep -c "UpgradeButton" app/account/page.tsx` >= 1 (free-user CTA)
    - `grep -c 'redirect("/login")' app/account/page.tsx` == 1
    - `grep -c "/account" components/AuthNav.tsx` >= 1 (nav link added)
    - AuthNav still guards demo: `grep -c "isSupabaseConfigured" components/AuthNav.tsx` >= 1
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>/account (server) shows email + current plan and renders the colocated `ManageSubscriptionButton` client component for subscribers (posting to the portal route) plus unlock CTAs for free users; the button lives in its own "use client" file, not in the server page; AuthNav links logged-in users to /account; unauthenticated hitting /account redirects to /login. Build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    /account page (email + plan + Manage subscription / unlock CTA), an authenticated /api/stripe/portal route, and an /account link in the top nav.
  </what-built>
  <how-to-verify>
    PREREQUISITE (one-time founder dashboard action — Stripe Customer Portal cannot open in test mode until this is saved):
      1. In the Stripe Dashboard in TEST mode, open Settings → Billing → Customer portal and click Save.
    Then:
      2. Ensure STRIPE_SECRET_KEY + Supabase env are set in .env.local, run `npm run dev`.
      3. Log in, open the /account link from the nav. Confirm your email + current plan show.
      4. If you are already Pro/Creator (via a prior Plan 02 test payment): click "Manage subscription" → you should land on the Stripe-hosted Customer Portal, then be returned to /account.
      5. As a free user: confirm the Unlock Pro / Go Creator CTAs appear instead of the portal button.
      6. Demo regression: stop the server, blank STRIPE_SECRET_KEY, restart — /account should still load and POST /api/stripe/portal should return a demo notice (no 500).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what broke.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/stripe/portal | Caller must only open a portal for their OWN subscription |
| /account (server) → profiles | Reads the authenticated user's plan; SELECT-own already allowed by RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Elevation of Privilege (IDOR) | app/api/stripe/portal/route.ts | mitigate | customer id derived from `auth.getUser()` → own profiles row; never from request body |
| T-02-09 | Info Disclosure | /account | mitigate | Unauthenticated access redirects to /login; only own email/plan rendered |
| T-02-10 | Denial of Service | portal route (no customer) | accept | No-customer case returns a pricing redirect, not a Stripe call — low value |
</threat_model>

<verification>
- `npx tsc --noEmit` clean, `npm run build` succeeds.
- Founder checkpoint (Task 3) covers the live portal round-trip (requires the one-time test-mode portal save) + demo regression.
</verification>

<success_criteria>
- /account shows plan + email; subscribers reach the Stripe Portal; free users see unlock CTAs.
- Portal route is authenticated and IDOR-safe (own customer id only).
- Manage-subscription button lives in its own client module; the account page stays a server component.
- AuthNav links to /account; demo mode never 500s.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stripe-webhook-pro-unlock/02-03-SUMMARY.md`.
</output>
