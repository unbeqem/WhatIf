---
phase: 03-polish-new-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Faq.tsx
  - components/Testimonials.tsx
  - components/SimulateForm.tsx
  - components/Nav.tsx
  - app/page.tsx
autonomous: true
requirements: [CONTENT-01, CONTENT-02, CONTENT-03]
must_haves:
  truths:
    - "The landing page shows a FAQ addressing data privacy, accuracy disclaimer, refund policy, and GDPR contact"
    - "The FAQ is an accordion that expands/collapses, keyboard-operable, and honors prefers-reduced-motion"
    - "The landing page shows 3-5 testimonial cards plus an honest product-truth stat counter that counts up when scrolled into view"
    - "No fabricated usage/user count appears anywhere on the landing page (hero or testimonials)"
    - "The nav includes an FAQ link pointing to /#faq"
    - "/decision presents ~8 visceral, clip-ready example prompts replacing the old 5"
  artifacts:
    - path: "components/Faq.tsx"
      provides: "Accordion FAQ with 4 objection rows, motion/react panel, aria-expanded"
      min_lines: 40
    - path: "components/Testimonials.tsx"
      provides: "Scroll-animated honest product-truth counter + 3-5 testimonial cards from a TESTIMONIALS data array"
      min_lines: 40
    - path: "components/SimulateForm.tsx"
      provides: "EXAMPLES array expanded to ~8 prompts"
      contains: "EXAMPLES"
  key_links:
    - from: "app/page.tsx"
      to: "components/Faq.tsx"
      via: "import + <section id='faq'> rendered between Pricing and Final CTA"
      pattern: "<Faq"
    - from: "app/page.tsx"
      to: "components/Testimonials.tsx"
      via: "import + <section id='proof'> rendered between Demo and Pricing"
      pattern: "<Testimonials"
    - from: "components/Nav.tsx"
      to: "/#faq"
      via: "LINKS array entry"
      pattern: "/#faq"
---

<objective>
Ship the landing-page conversion surfaces and TikTok-native prompt refresh: an accordion FAQ (privacy, accuracy, refund, GDPR), a testimonials block with a scroll-triggered animated product-truth counter, an FAQ nav link, and ~8 rewritten example prompts on /decision. Also purge the fabricated "10,247 decisions simulated" usage claim from the existing hero.

Purpose: Resolve objections right after price, warm the visitor with honest social proof before price, and make the example chips feel like real high-stakes decisions that read well in a TikTok recording — without any fake usage numbers.
Output: components/Faq.tsx, components/Testimonials.tsx, edits to app/page.tsx, components/Nav.tsx, components/SimulateForm.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-new-features/03-UI-SPEC.md

<interfaces>
<!-- Patterns to reuse verbatim. No codebase exploration required. -->

app/page.tsx structure (section order): Nav → HERO → PROBLEM → HOW IT WORKS (#how-it-works)
→ DEMO (#demo) → PRICING (#pricing) → FINAL CTA → Footer.
- Insert <Testimonials/> as a new <section id="proof"> BETWEEN the DEMO section and PRICING.
- Insert <Faq/> as a new <section id="faq"> BETWEEN PRICING and FINAL CTA.
- Existing section wrapper rhythm: `<section ...><div className="mx-auto max-w-6xl px-6 py-20 md:py-28">...`
- Existing eyebrow pattern: `<div className="mb-3 font-mono text-xs uppercase tracking-[0.22em] text-cyan-glow">…</div>` (alternates violet-glow / cyan-glow per section).
- Existing hero COUNTERS array (app/page.tsx lines 8-12) — CONTAINS THE FABRICATED CLAIM to remove:
  ```typescript
  const COUNTERS = [
    { value: "10,247", label: "decisions simulated" },   // <-- FABRICATED usage count; remove/replace
    { value: "3", label: "futures per question" },
    { value: "< 8s", label: "to your answer" },
  ];
  ```
- Existing card pattern (HOW IT WORKS steps): `rounded-2xl border border-border bg-surface/40 p-7 transition-colors hover:border-border-hi`.
- Existing scenario reveal cadence (ResultView): motion `initial={{opacity:0,y:20}}` whileInView `{opacity:1,y:0}` viewport `{once:true, margin:"-80px"}` transition `{duration:0.5, delay:i*0.1}`.

components/Nav.tsx LINKS array (lines 9-13):
```typescript
const LINKS = [
  { href: "/#how-it-works", label: "How it works" },
  { href: "/#demo", label: "Demo" },
  { href: "/#pricing", label: "Pricing" },
];
```
Add { href: "/#faq", label: "FAQ" } — keep to 4 items max (UI-SPEC caps nav at 4).

components/SimulateForm.tsx EXAMPLES array (lines 8-14) — replace contents (keep the const name, the chip render, and the "Need a starting point? Try one of these." lead-in).

Tokens available (globals.css — use ONLY these, no new tokens): bg bg-soft surface surface-hi;
border border-hi; fg fg-soft fg-mute; violet violet-glow cyan cyan-glow magenta amber;
utilities gradient-text glow-violet glow-cyan animate-pulse-glow. motion/react installed.
lucide-react installed (ChevronDown for the accordion).

CONTENT HONESTY (founder decision): NO fabricated usage/user counts anywhere on the page.
The animated counter must be an HONEST product truth (e.g. the 3 futures projected per
decision, the sub-8s speed, the free-to-try price) — never an invented "N decisions simulated"
or "N users" figure. This applies to BOTH the new testimonials counter AND the existing hero.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FAQ accordion (CONTENT-01) + Testimonials + honest scroll counter (CONTENT-02) components</name>
  <read_first>
    - 03-UI-SPEC.md section 1 "FAQ section" (accordion layout, per-row structure, motion AnimatePresence height/opacity, prefers-reduced-motion, aria-expanded/aria-controls, focus ring, the 4 REQUIRED objections with exact answer copy)
    - 03-UI-SPEC.md section 2 "Testimonials + animated counter" (counter strip useInView count-up fires once, reduced-motion shows final value, testimonial card structure + stagger reveal, content-honesty rules)
    - 03-UI-SPEC.md "Copywriting Contract" (eyebrow "What people say", counter labels) + footer contact address
    - components/ResultView.tsx (motion whileInView + viewport once pattern to mirror)
  </read_first>
  <files>components/Faq.tsx, components/Testimonials.tsx</files>
  <action>
1. Create `components/Faq.tsx` ("use client" — needs open/close state + motion). Render a `max-w-3xl mx-auto` accordion of the FOUR objection rows (exact copy from UI-SPEC section 1):
   - "Is my decision private?" → "We store your email and a subscription reference, nothing else. Your decision text is sent to the model to generate the simulation and is not saved to a profile or used for training."
   - "How accurate are the probabilities?" → "WhatIf is a decision-thinking tool, not a fortune teller. The three futures and probabilities are AI projections to widen your perspective — the final call is always yours."
   - "Can I cancel or get a refund?" → "Cancel anytime from your account in two clicks; you keep access until the period ends. No lock-in, no hidden tiers."
   - "You're in the EU — who do I contact about my data?" → "We're an EU-based operation and GDPR-compliant. Email data requests (access, deletion) to the contact address in the footer and we'll action them." — this row MUST include a real `mailto:` link. Use `mailto:hello@whatif.app` (matches the existing Footer Contact link).
   - Row container: `rounded-2xl border border-border bg-surface/40` with `divide-y divide-border/60`.
   - Trigger: full-width `button`, `flex items-center justify-between px-5 py-4`, question in `font-display text-xl leading-tight`, lucide `ChevronDown` (`h-5 w-5 text-fg-mute`) rotating 180° on open (`transition-transform` + conditional `rotate-180`). Set `aria-expanded` and `aria-controls` → panel `id`; visible focus: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-glow/60`.
   - Panel: `motion/react` `AnimatePresence`, `initial={{height:0,opacity:0}}` animate `{height:"auto",opacity:1}` exit `{height:0,opacity:0}`; answer in `text-sm md:text-base leading-relaxed text-fg-soft px-5 pb-5`. Track open index in state (single-open or multi-open both fine); default all collapsed (first MAY start open).
   - Honor `prefers-reduced-motion`: read `useReducedMotion()` from motion/react; when reduced, render the panel with no height animation (snap open) — final content shown immediately.
   - Export default the component; it renders the accordion only (the page supplies the <section> wrapper + eyebrow + headline).
2. Create `components/Testimonials.tsx` ("use client" — counter uses useInView). Two sub-blocks.

   FIRST, at the top of the file (above the component), declare the seed data as named module-level constants so values + counts are trivially auditable and adjustable:
   ```typescript
   // Animated counter target. HONEST product truth — NOT a fabricated usage/user
   // count (founder decision). This is the number of futures WhatIf projects for
   // every decision, which is the core product promise. Single source of truth.
   const FUTURES_PER_DECISION = 3;
   const FUTURES_LABEL = "possible futures, every time";

   type Testimonial = { quote: string; name: string; role: string };
   // 3-5 entries (CONTENT-02). Pre-launch marketing copy: believable + specific,
   // abstract gradient avatars (no photos), locations from the hero trust strip.
   const TESTIMONIALS: Testimonial[] = [
     { quote: "I almost signed the lease — WhatIf showed me the 18-month version I hadn't pictured.", name: "…", role: "…, Berlin" },
     { quote: "…", name: "…", role: "…, Lisbon" },
     { quote: "…", name: "…", role: "…, New York" },
   ];
   ```
   Provide 3 (up to 5) real, believable quotes/names/locations following the UI-SPEC content-honesty rules — do NOT fabricate photos or full-name endorsements; locations from Berlin, Lisbon, New York, London, Tokyo.

   - Counter strip: 3 stat items, `font-display` numerals `text-5xl md:text-6xl` (the animated stat MAY use `gradient-text`), label under in `text-xs uppercase tracking-[0.15em] text-fg-mute`. Use HONEST product-truth stats — NO fabricated usage/user counts. The three items: (a) the animated stat = `FUTURES_PER_DECISION` (3) with label `FUTURES_LABEL` ("possible futures, every time"); (b) "< 8s" with label "to your answer" (static); (c) one more product-true stat consistent with the UI-SPEC content-honesty rule (e.g. "€0" with label "to try your first" or "3-in-1" best/likely/worst) — it must NOT imply a fake user/usage volume. The count-up: use motion/react `useInView(ref, { once: true })` + an animated number that counts 0 → `FUTURES_PER_DECISION` over ~1.2s easeOut, firing once on scroll-in. Under `useReducedMotion()`, show the final number immediately (no count-up). Wrap on mobile: `flex flex-wrap justify-center gap-x-10 gap-y-6`.
   - Testimonial cards: map over `TESTIMONIALS` into a `grid gap-5 md:grid-cols-3`. Each card `rounded-2xl border border-border bg-surface/40 p-6 hover:border-border-hi` with an optional top gradient hairline `absolute inset-x-0 top-0 h-px bg-gradient-to-r from-violet to-cyan`. Contents: quote in `font-display text-xl leading-snug text-fg`; attribution row = gradient avatar dot `h-8 w-8 rounded-full bg-gradient-to-br from-violet via-magenta to-cyan` + name `text-sm text-fg` + role/location `text-xs text-fg-mute`.
   - Stagger reveal: `motion` cards `initial={{opacity:0,y:20}}` `whileInView={{opacity:1,y:0}}` `viewport={{once:true,margin:"-80px"}}` `transition={{duration:0.5, delay:i*0.1}}` (mirror ResultView).
   - Export default; the page supplies the <section> wrapper + eyebrow "What people say".
   - Use ONLY existing globals.css tokens.
   </action>
  <verify>
    <automated>cd "C:/Users/tristan/Documents/MoneyMoneyMoney/WhatIf" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"use client"' components/Faq.tsx` AND `grep -q '"use client"' components/Testimonials.tsx`.
    - Faq covers all four objections: `grep -qi 'private' components/Faq.tsx` AND `grep -qi 'accurate' components/Faq.tsx` AND `grep -qi 'refund\|cancel' components/Faq.tsx` AND `grep -qi 'GDPR\|EU-based' components/Faq.tsx`.
    - `grep -q 'mailto:hello@whatif.app' components/Faq.tsx` (GDPR contact is a real mailto).
    - `grep -q 'aria-expanded' components/Faq.tsx` AND `grep -q 'aria-controls' components/Faq.tsx` AND `grep -q 'ChevronDown' components/Faq.tsx`.
    - `grep -q 'useReducedMotion' components/Faq.tsx` AND `grep -q 'useReducedMotion' components/Testimonials.tsx`.
    - `grep -q 'useInView' components/Testimonials.tsx` (scroll-triggered counter).
    - Counter target is a single named HONEST constant: `grep -c 'FUTURES_PER_DECISION' components/Testimonials.tsx` returns >= 2 (declared once, referenced at least once).
    - No fabricated usage count in the component: `grep -c '10,247\|10247\|decisions simulated' components/Testimonials.tsx` returns 0.
    - Testimonials has 3-5 cards, machine-checkable via the data array: count the `quote:` entries inside the `TESTIMONIALS` array — `grep -c 'quote:' components/Testimonials.tsx` returns a value between 3 and 5 inclusive. (Mirrors how Task 2 gates the 8-prompt count.)
    - No new tokens: `grep -cE 'text-\[#|bg-\[#' components/Faq.tsx` returns 0 AND same for Testimonials.tsx.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>FAQ accordion (4 objections, a11y, reduced-motion) and Testimonials (honest count-up from FUTURES_PER_DECISION + 3-5 cards from TESTIMONIALS array, no fabricated usage count) components compile and use only existing tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Mount FAQ + Testimonials in page.tsx, purge fabricated hero counter, add FAQ nav link, rewrite example prompts (CONTENT-03)</name>
  <read_first>
    - app/page.tsx (section order — insert points: Testimonials between DEMO and PRICING, FAQ between PRICING and FINAL CTA; reuse the `mx-auto max-w-6xl px-6 py-20 md:py-28` wrapper + eyebrow pattern; the hero COUNTERS array at lines 8-12 holding the fabricated "10,247 decisions simulated")
    - components/Nav.tsx LINKS array (lines 9-13) — add /#faq, keep 4 items
    - components/SimulateForm.tsx EXAMPLES (lines 8-14) — replace with ~8 prompts
    - 03-UI-SPEC.md section 5 "Example-prompt chips" (the suggested 8-prompt set, <=70 chars each, keep the lead-in) and sections 1/2 placement notes
  </read_first>
  <files>app/page.tsx, components/Nav.tsx, components/SimulateForm.tsx</files>
  <action>
1. Edit `app/page.tsx`:
   - Import `Testimonials` and `Faq` at the top: `import Testimonials from "@/components/Testimonials";` `import Faq from "@/components/Faq";`
   - Insert a `<section id="proof">` between the DEMO section (`id="demo"`) and the PRICING section (`id="pricing"`). Use the standard wrapper `mx-auto max-w-5xl px-6 py-20 md:py-28`, a centered header block (eyebrow `font-mono text-xs uppercase tracking-[0.22em] text-violet-glow` reading "What people say", a `font-display text-4xl md:text-5xl` headline), then `<Testimonials />`.
   - Insert a `<section id="faq">` between the PRICING section and the FINAL CTA section. Wrapper `mx-auto max-w-6xl px-6 py-20 md:py-28`, centered header (eyebrow color `text-cyan-glow` reading "FAQ" or "Questions", headline), then `<Faq />`.
   - PURGE THE FABRICATED CLAIM: in the hero `COUNTERS` array (lines 8-12), the entry `{ value: "10,247", label: "decisions simulated" }` is a fabricated usage number (founder decision: no fake usage/user counts). Replace it with an HONEST product-truth stat consistent with the testimonials counter — recommended: `{ value: "3", label: "possible futures, every time" }`. If that duplicates the existing `{ value: "3", label: "futures per question" }` entry, collapse the two into one honest "3 / possible futures, every time" item and add a different product-true stat in the freed slot (e.g. `{ value: "€0", label: "to try your first" }`) — keep the hero at 3 counter items. The result: the literal "10,247"/"10247" and the "decisions simulated" usage claim MUST be gone from the file.
   - Do not otherwise alter existing sections' content.
2. Edit `components/Nav.tsx`: add `{ href: "/#faq", label: "FAQ" }` to the LINKS array. Total stays at 4 items — the UI-SPEC caps the nav at 4, and FAQ takes the new slot (do NOT add a "proof" link).
3. Edit `components/SimulateForm.tsx`: replace the EXAMPLES array (keep the `const EXAMPLES = [...]` name and everything else) with ~8 visceral, clip-ready first-person prompts, each <=70 chars. Use this set (executor may refine wording but keep the high-stakes TikTok-hook register and the count of 8):
   - "Should I quit my stable job to go all-in on my side project?"
   - "Should I move across the world for someone I've known six months?"
   - "Should I end a five-year relationship that's just… fine?"
   - "Should I take the €40k offer or counter and risk losing it?"
   - "Should I have a kid this year or wait two more?"
   - "Should I drop out to build this, or finish the degree?"
   - "Should I cut off a family member for my own sanity?"
   - "Should I sink my savings into this apartment or keep renting?"
   Keep the "Need a starting point? Try one of these." lead-in and the existing chip styling unchanged.
   </action>
  <verify>
    <automated>cd "C:/Users/tristan/Documents/MoneyMoneyMoney/WhatIf" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import Testimonials' app/page.tsx` AND `grep -q '<Testimonials' app/page.tsx`.
    - `grep -q 'import Faq' app/page.tsx` AND `grep -q '<Faq' app/page.tsx`.
    - `grep -q 'id="proof"' app/page.tsx` AND `grep -q 'id="faq"' app/page.tsx`.
    - Fabricated hero claim purged: `grep -c '10,247\|10247' app/page.tsx` returns 0 AND `grep -c 'decisions simulated' app/page.tsx` returns 0.
    - `grep -q '/#faq' components/Nav.tsx` AND the LINKS array still has exactly 4 entries (`grep -c 'href: "/#' components/Nav.tsx` returns 4).
    - EXAMPLES has ~8 entries: within the EXAMPLES array in SimulateForm.tsx there are 8 quoted prompt strings (count the lines between `const EXAMPLES = [` and the closing `];`; must be 8).
    - `grep -q 'Need a starting point' components/SimulateForm.tsx` (lead-in preserved).
    - `npx tsc --noEmit` exits 0; `npm run build` succeeds.
  </acceptance_criteria>
  <done>Landing renders the testimonials/proof + FAQ sections in the right slots, the fabricated "10,247 decisions simulated" hero claim is gone, nav has an FAQ link (4 total), /decision shows 8 new prompts; build + typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds static landing content + client-only animations. No new untrusted input crosses a boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | components/Faq.tsx | accept | FAQ copy is founder-approved public marketing; GDPR mailto is the already-public footer address. No secrets. |
| T-03-06 | Tampering | components/Testimonials.tsx | accept | Seeded testimonial data is static in-component; no user input, no persistence, no injection surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` — no type errors.
- `npm run build` — Next build compiles the new sections + client components.
- FAQ: all four objection topics present (privacy, accuracy, refund/cancel, GDPR) with a real mailto; accordion keyboard-operable (aria-expanded/aria-controls, focus ring); prefers-reduced-motion honored.
- Testimonials: 3-5 cards (TESTIMONIALS array) + an HONEST counter that counts up to FUTURES_PER_DECISION on scroll-into-view (once) and shows final value under reduced motion. No fabricated usage count.
- Hero: the fabricated "10,247 decisions simulated" is removed/replaced with an honest product-truth stat (`grep -c '10,247\|10247' app/page.tsx` == 0).
- Nav: /#faq link present, 4 items total.
- /decision: 8 example prompts.
- No new tokens (only globals.css vocabulary), no new dependencies.
</verification>

<success_criteria>
- CONTENT-01: Landing FAQ addresses data privacy, accuracy disclaimer, refund policy, GDPR contact in plain language, placed between Pricing and Final CTA.
- CONTENT-02: 3-5 testimonial cards + an honest product-truth counter that animates on scroll, placed between Demo and Pricing, readable in a screen recording. No fabricated usage/user counts anywhere on the page.
- CONTENT-03: /decision shows ~8 visceral clip-ready prompts replacing the old 5.
- Uses only existing globals.css tokens; no new dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-new-features/03-02-SUMMARY.md`
</output>
