---
phase: 03-polish-new-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/Inter-SemiBold.ttf
  - assets/InstrumentSerif-Regular.ttf
  - lib/export-gate.ts
  - components/StoryCard.tsx
  - app/api/export/route.tsx
  - components/ShareCard.tsx
  - components/ResultView.tsx
  - tests/export-gate.test.ts
  - tests/export-render.test.ts
  - vitest.config.ts
autonomous: true
requirements: [EXPORT-01, EXPORT-02, EXPORT-03, CONTENT-04]
must_haves:
  truths:
    - "A Creator user on /result clicks 'Download story card' and receives a 1080x1920 PNG named whatif-simulation.png"
    - "The PNG shows the WhatIf wordmark, the user's question, the recommendation, and the three scenario tags with probabilities"
    - "A free or pro user calling POST /api/export gets HTTP 402 with a JSON upsell payload, never an image or silent failure"
    - "The /result Share tile shows the Creator download for creator plan and the Creator upsell for free/pro/anon"
    - "When Supabase is unconfigured (demo mode), the export renders (treated as Creator) so marketing footage works"
  artifacts:
    - path: "app/api/export/route.tsx"
      provides: "Node.js runtime export route: creator-gate + 402 + ImageResponse PNG"
      contains: 'runtime = "nodejs"'
    - path: "components/StoryCard.tsx"
      provides: "Satori-safe 1080x1920 card JSX (inline styles, hex colors)"
      min_lines: 40
    - path: "lib/export-gate.ts"
      provides: "Pure resolveExportPlan + gate decision, unit-testable"
      exports: ["exportGateDecision"]
    - path: "components/ShareCard.tsx"
      provides: "Client Share/Download control: POSTs payload, branches on 402, downloads blob"
      min_lines: 40
    - path: "assets/Inter-SemiBold.ttf"
      provides: "Bundled Inter font binary for ImageResponse"
    - path: "assets/InstrumentSerif-Regular.ttf"
      provides: "Bundled Instrument Serif font binary for ImageResponse"
  key_links:
    - from: "components/ResultView.tsx"
      to: "components/ShareCard.tsx"
      via: "import + mount in the bottom action grid, passed input/result/me"
      pattern: "ShareCard"
    - from: "components/ShareCard.tsx"
      to: "/api/export"
      via: "fetch POST { input, result }, branch on res.status === 402 before res.blob()"
      pattern: "fetch\\(.*api/export"
    - from: "app/api/export/route.tsx"
      to: "supabaseAdmin.profiles.plan"
      via: "createSupabaseServerClient().auth.getUser() then supabaseAdmin.from('profiles').select('plan')"
      pattern: "from\\(\"profiles\"\\)"
---

<objective>
Build the Creator-tier 9:16 story-card export end to end: a Node.js route that renders a branded 1080x1920 PNG via next/og ImageResponse, gated to plan='creator' (402 + upsell for everyone else), plus the /result Share affordance that drives the download for Creator and the upsell for free/pro.

Purpose: The story card is the Creator differentiator and the growth loop — every reshared card is an ad. The 402 gate is the paywall that sells Creator.
Output: assets/ font binaries, lib/export-gate.ts, components/StoryCard.tsx, app/api/export/route.tsx, components/ShareCard.tsx, ResultView wiring, and two vitest specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-new-features/03-RESEARCH.md
@.planning/phases/03-polish-new-features/03-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

From lib/types.ts:
```typescript
export type Scenario = {
  title: string;
  tag: "Best Case" | "Likely" | "Worst Case" | string;
  probability: number;
  short_term: string;
  long_term: string;
  key_risk: string;
};
export type SimulationResult = {
  scenarios: Scenario[];
  most_likely: string;
  recommendation: string;
  reasoning: string;
  demo?: boolean;
};
```

From lib/useMe.ts:
```typescript
export type Me = {
  configured: boolean;
  authenticated: boolean;
  email: string | null;
  plan: "free" | "pro" | "creator" | null;
};
export function useMe(): Me | undefined;          // undefined while loading
export function isSubscriberPlan(plan: Me["plan"]): boolean; // pro || creator
```

From lib/supabase/server.ts:
```typescript
export async function createSupabaseServerClient(): Promise<SupabaseClient>;
```
From lib/supabase/admin.ts:
```typescript
export const supabaseAdmin: SupabaseClient | null; // null when SUPABASE_SERVICE_ROLE_KEY / URL unset
export const isSupabaseAdminConfigured: boolean;
```

The EXACT plan-read pattern to reuse (from app/api/me/route.ts) — note the null-guard on
supabaseAdmin: it can be null even when NEXT_PUBLIC_SUPABASE_URL is set (partial config,
SUPABASE_SERVICE_ROLE_KEY missing). The guard makes partial config fall through to "free":
```typescript
const supabase = await createSupabaseServerClient();
const { data } = await supabase.auth.getUser();
const user = data?.user;
let plan = "free";
if (supabaseAdmin) {
  const { data: profile } = await supabaseAdmin
    .from("profiles").select("plan").eq("id", user.id).maybeSingle();
  plan = profile?.plan ?? "free";
}
```

The demo-mode invariant (from lib/quota.ts:53-55): when supabaseAdmin is null, the loop fails OPEN (allow). Mirror this in the export gate: unconfigured Supabase → treat viewer as creator, render the card.

ResultView.tsx bottom action grid (lines 157-200): a `<div>` whose class is
`showUpsell ? "grid gap-4 md:grid-cols-3" : "grid gap-4"`. First child is the
"Ask another question" Link. The Pro upsell tile (md:col-span-2) renders only
when `showUpsell` (me !== undefined && !isSubscriberPlan(me.plan)). `me` is from
`const me = useMe();` already at the top of the component. `input` and `result`
come from destructured `data` (`const { input, result } = data;`).

Nav logo mark (from components/Nav.tsx:44-50) to reproduce in the card as brand:
gradient tile `bg-gradient-to-br from-violet via-magenta to-cyan` with a white "?" +
wordmark "What" + <span text-violet-glow>If</span>.

Token hex values (from app/globals.css, use these inline in StoryCard — Tailwind
classes do NOT work in ImageResponse):
bg #07060d · bg-soft #0d0a1a · surface #110d22 · surface-hi #1a1432 ·
border #271f4a (approx) · fg #f5f3ff · fg-soft #c9c6e6 (approx) · fg-mute #7d7aa3 ·
violet #a855f7 · violet-glow #c084fc · cyan #22d3ee · cyan-glow #67e8f9 ·
magenta #ec4899 · amber #fbbf24
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle fonts + pure export-gate helper + wire vitest, with failing tests (Wave 0 scaffold)</name>
  <read_first>
    - 03-RESEARCH.md "Validation Architecture" (Wave 0 Gaps) + "Pattern 1" (font load via fs.readFile from assets/)
    - lib/quota.ts (demo fail-open pattern, PlanTier import from lib/db.types)
    - vitest.config.ts (current include is only lib/**/*.test.ts — MUST extend to tests/**)
    - app/api/me/route.ts (the plan-read pattern the gate helper mirrors)
  </read_first>
  <files>assets/Inter-SemiBold.ttf, assets/InstrumentSerif-Regular.ttf, lib/export-gate.ts, tests/export-gate.test.ts, vitest.config.ts</files>
  <action>
1. Create the `assets/` dir and download the two REQUIRED font binaries from these PINNED, verified-reachable URLs (both returned HTTP 200 and a valid TTF at plan time; each well under the 500KB OG bundle budget). Download with `curl -fL -o <path> <url>` (the `-f` flag makes curl exit non-zero on HTTP 4xx/5xx so a 404 fails loudly instead of writing an error page):
   - `assets/Inter-SemiBold.ttf` — Inter weight 600, latin subset (~68KB TTF):
     `https://cdn.jsdelivr.net/fontsource/fonts/inter@latest/latin-600-normal.ttf`
   - `assets/InstrumentSerif-Regular.ttf` — Instrument Serif regular (~70KB TTF):
     `https://raw.githubusercontent.com/google/fonts/main/ofl/instrumentserif/InstrumentSerif-Regular.ttf`
   VALIDATE each downloaded file before proceeding: `file <path>` must report "TrueType"/"OpenType" AND `wc -c <path>` must be > 20000 bytes. If EITHER download fails the curl fetch (non-zero exit) or fails this validation (wrong type / < 20KB / zero bytes), STOP: do not proceed to later tasks with a broken or zero-byte font — surface a BLOCKER to the coordinator stating which font URL failed and its curl exit / `file` output, so a human can supply a vendored `.ttf` (commit the two binaries directly under `assets/`) or an alternate pinned URL. A broken font silently degrades the card to a system font and breaks the whole export deliverable, so a bad download is a hard stop, not a fallback.
   Do NOT add any npm dependency. Do NOT use a `.woff2` (ImageResponse accepts only ttf/otf/woff).
2. Create `lib/export-gate.ts` exporting a PURE, dependency-free decision function (no Supabase import here — keep it unit-testable):
   ```typescript
   export type ExportGate =
     | { ok: true }
     | { ok: false; status: 402; body: { error: "upgrade_required"; plan: string; upsell: { target: "creator"; price: "€9/mo"; href: "/#pricing" } } };

   // demoMode === true means Supabase is unconfigured -> allow (treat as creator), per lib/quota fail-open.
   export function exportGateDecision(plan: string | null, demoMode: boolean): ExportGate {
     if (demoMode || plan === "creator") return { ok: true };
     return {
       ok: false,
       status: 402,
       body: { error: "upgrade_required", plan: plan ?? "free", upsell: { target: "creator", price: "€9/mo", href: "/#pricing" } },
     };
   }
   ```
3. Extend `vitest.config.ts` so `tests/**` runs AND the `@/` path alias resolves (the specs import from `@/lib/export-gate`):
   ```typescript
   import { defineConfig } from "vitest/config";
   import { fileURLToPath } from "node:url";
   export default defineConfig({
     resolve: { alias: { "@": fileURLToPath(new URL(".", import.meta.url)) } },
     test: { include: ["lib/**/*.test.ts", "tests/**/*.test.ts"], environment: "node" },
   });
   ```
4. Create `tests/export-gate.test.ts` (EXPORT-03) covering: creator → ok:true; free → 402 with error "upgrade_required", plan "free", upsell.target "creator", upsell.href "/#pricing"; pro → 402; null plan + demoMode true → ok:true; null plan + demoMode false → 402 with plan "free".
   </action>
  <verify>
    <automated>cd "C:/Users/tristan/Documents/MoneyMoneyMoney/WhatIf" && npm test -- tests/export-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f assets/Inter-SemiBold.ttf && test -f assets/InstrumentSerif-Regular.ttf` both exist; each > 20KB (`wc -c`).
    - `file assets/Inter-SemiBold.ttf` output contains "TrueType" (or "OpenType"); same for InstrumentSerif-Regular.ttf.
    - Fonts came from the pinned URLs above (no `.woff2`, no npm dependency); if either failed, the executor stopped with a BLOCKER rather than shipping a broken font.
    - `grep -c "export function exportGateDecision" lib/export-gate.ts` returns 1.
    - `grep -q 'tests/\*\*/\*.test.ts' vitest.config.ts` AND `grep -q '"@"' vitest.config.ts` (alias present).
    - `npm test -- tests/export-gate.test.ts` exits 0 with all cases passing.
    - `git diff package.json` is empty (no new dependency added).
  </acceptance_criteria>
  <done>Fonts bundled from pinned URLs (validated as real TTFs, else hard-stop blocker), pure gate helper exists, vitest runs tests/ with @ alias, export-gate spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StoryCard component + export route (next/og, nodejs runtime, gate, 402, demo-allow) + render smoke test</name>
  <read_first>
    - 03-RESEARCH.md "Pattern 1" (route skeleton), "Satori-safe card skeleton" (Code Examples), "Anti-Patterns to Avoid" (inline styles only, display:flex for >1 child, no backdrop-filter, no edge runtime), "Pitfall 1/3" (gradient-text fallback, lineClamp, which fields to render)
    - 03-UI-SPEC.md section 4 "Branded 9:16 story card" (full visual layout, safe margins 96px, top 220px / bottom 320px reserved bands, question >=64px, footer CTA)
    - lib/export-gate.ts (from Task 1 — import exportGateDecision)
    - app/api/me/route.ts (plan-read pattern — INCLUDING the `if (supabaseAdmin)` null-guard) + lib/supabase/admin.ts (supabaseAdmin can be null under partial config)
  </read_first>
  <files>components/StoryCard.tsx, app/api/export/route.tsx, tests/export-render.test.ts</files>
  <behavior>
    - POST with valid { input, result } as creator (or demo mode) → 200, Content-Type image/png, Content-Disposition attachment filename whatif-simulation.png, non-empty body.
    - POST as free/pro (Supabase configured, no creator) → 402, JSON body { error: "upgrade_required", plan, upsell: { target: "creator", price: "€9/mo", href: "/#pricing" } }.
    - POST with Supabase URL set but SUPABASE_SERVICE_ROLE_KEY missing (supabaseAdmin null, partial config) → plan resolves to "free" → 402 (NOT a 500 null-deref).
    - POST with missing/short input or result lacking recommendation → 400 { error: "bad_payload" }.
    - Supabase unconfigured (NEXT_PUBLIC_SUPABASE_URL unset) → render (demo-allow), 200 image/png.
  </behavior>
  <action>
1. Create `components/StoryCard.tsx` — a PURE function component (no "use client", no hooks, no Tailwind classes; imported only by the route). Props `{ input: string; result: SimulationResult }`. Follow 03-UI-SPEC section 4 layout at 1080x1920:
   - Root div: `display:"flex", flexDirection:"column", width:"100%", height:"100%", backgroundColor:"#07060d", padding:96, color:"#f5f3ff", fontFamily:"Inter", justifyContent:"space-between"`. Add the brand radial wash via layered `backgroundImage` `radial-gradient(...)` using the three brand rgba stops from the UI-SPEC (violet top-right rgba(168,85,247,0.22), cyan top-left rgba(34,211,238,0.14), magenta bottom rgba(236,72,153,0.12)). NO backdrop-filter/blur (Satori unsupported) — fake glow with box-shadow only.
   - Header band: gradient "?" tile (`backgroundImage:"linear-gradient(135deg,#a855f7,#ec4899,#22d3ee)"`, borderRadius 16, ~72px square, white "?") + wordmark "What" + a span color `#c084fc` "If", plus mono eyebrow "AI DECISION SIMULATION" color `#7d7aa3` letterSpacing wide.
   - Question block (center-upper): curly-quoted `input` in Instrument Serif, fontSize 64 (>=64 required), lineHeight 1.1, color #f5f3ff. Clamp to ~4 lines using `display:"-webkit-box", WebkitBoxOrient:"vertical", WebkitLineClamp` or Satori `lineClamp` — truncate long input.
   - Three futures: map `result.scenarios` into 3 rows/columns. Each: tag chip (mono ~24px, colored per tag — Best Case cyan #67e8f9, Likely violet-glow #c084fc, Worst Case magenta #ec4899), a probability bar (height 12px, borderRadius 999, background #271f4a, inner fill width `${Math.round(s.probability)}%` with a linear-gradient matching the tag), and the pct numeral in Instrument Serif ~40px. Any container with >1 child MUST set display:flex. Titles one line, truncate.
   - Recommendation strip (center-lower): mono eyebrow "THE CALL" color #c084fc, then `result.recommendation` in Instrument Serif ~44px, clamp to 3 lines.
   - Footer band: "whatif.app — simulate your decision" mono ~28px, color #c9c6e6, centered.
   - For any gradient TEXT, use a solid fallback color (e.g. #c084fc) — do NOT rely on backgroundClip:text (highest-risk Satori feature); solid color is the safe choice for legibility.
2. Create `app/api/export/route.tsx` (note `.tsx` — it returns JSX):
   - `export const runtime = "nodejs";` and `export const maxDuration = 30;`
   - `import { ImageResponse } from "next/og";` `import { readFile } from "node:fs/promises";` `import { join } from "node:path";`
   - `export async function POST(req: NextRequest)`:
     a. Parse body; validate: `input` is a string with trimmed length 8..1500 (reuse the /api/simulate 8–1500 discipline) and `result?.recommendation` + `Array.isArray(result?.scenarios)` present. Else `NextResponse.json({ error: "bad_payload" }, { status: 400 })`.
     b. Resolve plan with the EXACT me-route pattern, INCLUDING the supabaseAdmin null-guard (do NOT call `.from()` on a possibly-null client):
        - `const demoMode = !process.env.NEXT_PUBLIC_SUPABASE_URL;`
        - If `demoMode` → skip the Supabase read entirely, `plan = null`.
        - Else: `const supabase = await createSupabaseServerClient(); const { data } = await supabase.auth.getUser(); const user = data?.user;` then:
          ```typescript
          let plan = "free";
          if (user && supabaseAdmin) {
            const { data: profile } = await supabaseAdmin
              .from("profiles").select("plan").eq("id", user.id).maybeSingle();
            plan = profile?.plan ?? "free";
          }
          ```
          This mirrors app/api/me/route.ts exactly — `supabaseAdmin` is null when SUPABASE_SERVICE_ROLE_KEY is unset even if the URL is set, so the `if (supabaseAdmin)` guard makes partial config fall through to plan "free" (→ 402), never a null-deref/500. A missing user also stays "free" (still gated).
     c. `const gate = exportGateDecision(plan, demoMode);` if `!gate.ok` → `NextResponse.json(gate.body, { status: gate.status })` (402).
     d. Read fonts: `const [inter, serif] = await Promise.all([readFile(join(process.cwd(), "assets/Inter-SemiBold.ttf")), readFile(join(process.cwd(), "assets/InstrumentSerif-Regular.ttf"))]);`
     e. `return new ImageResponse(<StoryCard input={input} result={result} />, { width: 1080, height: 1920, fonts: [{ name: "Inter", data: inter, weight: 600, style: "normal" }, { name: "Instrument Serif", data: serif, weight: 400, style: "normal" }], headers: { "Content-Disposition": 'attachment; filename="whatif-simulation.png"' } });`
   - Do NOT set runtime edge. Do NOT fetch fonts over the network at render time. Do NOT add dependencies.
3. Create `tests/export-render.test.ts` (EXPORT-01/02) — a lightweight contract smoke test. Because ImageResponse needs the full Next runtime, keep it a UNIT-level assertion on the pure pieces plus the route's non-render branches that DON'T require ImageResponse: import the route's exported `runtime` and `maxDuration` consts and assert `runtime === "nodejs"` and `maxDuration === 30`; assert the StoryCard renders without throwing given a sample SimulationResult (call `StoryCard({...})` and assert it returns a truthy React element). Do NOT attempt to rasterize a PNG inside vitest (that path is the founder's manual visual check per RESEARCH). If importing route.tsx pulls server-only Supabase and breaks under vitest node env, instead assert the contract by reading the file: `expect(readFileSync("app/api/export/route.tsx","utf8")).toMatch(/runtime = "nodejs"/)` and `.toMatch(/width: 1080/)` and `.toMatch(/height: 1920/)`.
   </action>
  <verify>
    <automated>cd "C:/Users/tristan/Documents/MoneyMoneyMoney/WhatIf" && npm test -- tests/export-render.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'runtime = "nodejs"' app/api/export/route.tsx` returns 1.
    - `grep -q 'maxDuration = 30' app/api/export/route.tsx`.
    - `grep -q 'from "next/og"' app/api/export/route.tsx` AND `grep -q 'ImageResponse' app/api/export/route.tsx`.
    - `grep -q 'width: 1080' app/api/export/route.tsx` AND `grep -q 'height: 1920' app/api/export/route.tsx`.
    - `grep -q 'exportGateDecision' app/api/export/route.tsx` (gate reused, not re-implemented).
    - `grep -q 'if (supabaseAdmin)' app/api/export/route.tsx` (null-guard present — no unguarded `.from("profiles")` on a possibly-null client).
    - `grep -q 'attachment; filename=\"whatif-simulation.png\"' app/api/export/route.tsx`.
    - `grep -q 'readFile' app/api/export/route.tsx` AND `grep -q 'assets/Inter-SemiBold.ttf' app/api/export/route.tsx` (fonts bundled, not fetched). `grep -c 'fonts.googleapis' app/api/export/route.tsx` returns 0.
    - `grep -c 'runtime = "edge"' app/api/export/route.tsx` returns 0.
    - StoryCard.tsx: no `className=` on any element (`grep -c 'className=' components/StoryCard.tsx` returns 0) and no backdrop-filter (`grep -ci 'backdrop' components/StoryCard.tsx` returns 0).
    - `git diff package.json` is empty (no new dependency).
    - `npm test -- tests/export-render.test.ts` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Route renders a 1080x1920 PNG for creator/demo, returns 402 for free/pro AND for partial-config (supabaseAdmin null), 400 on bad payload, fonts embedded from assets/, contract smoke test green, typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: ShareCard client control + mount in ResultView (CONTENT-04, EXPORT-03 client half)</name>
  <read_first>
    - 03-UI-SPEC.md section 3 "'Share this simulation' affordance" (three states: creator download / non-creator upsell / existing Pro upsell coexistence; loading + success + 402 copy) and "Copywriting Contract" table
    - 03-RESEARCH.md "Pattern 3: Client download from a POST blob" (branch on 402 before res.blob())
    - components/ResultView.tsx (bottom action grid lines 157-200, showUpsell logic, me/input/result availability)
    - components/UpgradeButton.tsx (plan-based checkout button, used for the Creator upsell CTA)
    - lib/useMe.ts (Me type, isSubscriberPlan)
  </read_first>
  <files>components/ShareCard.tsx, components/ResultView.tsx</files>
  <action>
1. Create `components/ShareCard.tsx` ("use client"). Props: `{ input: string; result: SimulationResult; me: Me | undefined }`. Import `useMe` types, `UpgradeButton`, lucide `Download` + `Loader2`, `Link` from next/link.
   - If `me === undefined` → render nothing (matches the ResultView `showUpsell` no-flash discipline).
   - If `me.plan === "creator"` → the ACTIVE Creator tile. Heading `font-display text-xl`: "Turn this into content." Sub `text-sm text-fg-soft`: "Export a 9:16 card for TikTok, Reels, or Stories." Primary button (violet→magenta gradient, `rounded-xl`, `Download` icon `h-4 w-4`) label "Download story card". On click:
     ```typescript
     const res = await fetch("/api/export", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ input, result }) });
     if (res.status === 402) { setUpsell(true); return; }   // surface the upsell inline, never silent
     if (!res.ok) { setError("Couldn't render the card. Try again in a moment."); return; }
     const blob = await res.blob();
     const url = URL.createObjectURL(blob);
     const a = document.createElement("a"); a.href = url; a.download = "whatif-simulation.png"; a.click();
     URL.revokeObjectURL(url); setSaved(true); setTimeout(() => setSaved(false), 3000);
     ```
     Loading state: disabled button, `Loader2` `animate-spin`, label "Rendering your card…". Success: inline "Saved — check your downloads" in `text-cyan-glow text-xs`, auto-clear ~3s. If the 402 branch fires mid-session, render the upsell copy: "Story-card export is a Creator feature." + the Creator CTA (UpgradeButton plan="creator").
   - Else (free / pro / anon, i.e. NOT creator) → the UPSELL tile. Same visual slot as the Creator tile. Heading "Want to share this?" Sub "Creator turns any simulation into a branded 9:16 card, built for the For You page." Primary CTA `<UpgradeButton plan="creator">Unlock Creator — €9/mo</UpgradeButton>` + a ghost "Compare plans" Link to `/#pricing` (mirror the existing Pro upsell secondary link styling: `border-border-hi bg-bg/40 ...`).
   - Tile container styling reuses the existing upsell tile look: `relative overflow-hidden rounded-2xl border border-violet-glow/40 bg-gradient-to-br from-violet/15 to-surface/60 p-5`.
   - Use ONLY existing globals.css tokens — do not add tokens.
2. Edit `components/ResultView.tsx` bottom action grid (currently lines ~157-200):
   - Import ShareCard at top: `import ShareCard from "@/components/ShareCard";`
   - The grid must now host: [Ask another question] + [existing Pro upsell — free users only] + [ShareCard]. Update grid columns so it stays balanced:
     - Keep the existing Pro upsell tile exactly as-is but gate it to free/anon only. Since Pro users are subscribers, the Pro upsell already hides via `showUpsell` (showUpsell is false for pro AND creator). That is correct — leave the Pro-upsell block wrapped in `{showUpsell && (...)}` unchanged.
     - Mount `<ShareCard input={input} result={result} me={me} />` as an additional grid child AFTER the Pro upsell block. ShareCard self-hides for creator's upsell vs download internally, and self-hides entirely while `me === undefined`.
     - Adjust the grid container class: when `showUpsell` is true (free/anon) there are 3 tiles → keep `md:grid-cols-3`. When `showUpsell` is false: for a Creator user ShareCard shows the download tile → use `md:grid-cols-2`; for a Pro user ShareCard shows the Creator upsell tile → `md:grid-cols-2`. Compute: `const gridCols = showUpsell ? "md:grid-cols-3" : (me ? "md:grid-cols-2" : "");` and apply `className={\`grid gap-4 ${gridCols}\`}`.
   - Do not remove or restyle the existing "Ask another question" Link or the Pro upsell copy.
   </action>
  <verify>
    <automated>cd "C:/Users/tristan/Documents/MoneyMoneyMoney/WhatIf" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import ShareCard' components/ResultView.tsx` AND `grep -q '<ShareCard' components/ResultView.tsx`.
    - `grep -q 'input={input}' components/ResultView.tsx` context around ShareCard (props passed): `grep -q 'result={result}' components/ResultView.tsx` AND `grep -q 'me={me}' components/ResultView.tsx`.
    - `grep -q '"use client"' components/ShareCard.tsx`.
    - `grep -q 'res.status === 402' components/ShareCard.tsx` (402 branch before blob).
    - `grep -q 'Download story card' components/ShareCard.tsx` AND `grep -q 'Unlock Creator — €9/mo' components/ShareCard.tsx` AND `grep -q 'Rendering your card' components/ShareCard.tsx`.
    - `grep -q 'me === undefined' components/ShareCard.tsx` (no-flash guard).
    - `grep -q 'UpgradeButton plan="creator"' components/ShareCard.tsx`.
    - No new tokens: `grep -cE 'text-\[#|bg-\[#' components/ShareCard.tsx` returns 0.
    - `npx tsc --noEmit` exits 0; `npm run build` succeeds.
  </acceptance_criteria>
  <done>/result shows Creator download for creator, Creator upsell for free/pro, coexisting with the existing Pro upsell for free users; 402 surfaces inline as upsell; build + typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/export | Untrusted POST body ({ input, result }) and untrusted implicit plan claim cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | app/api/export/route.tsx | mitigate | Server re-reads `profiles.plan` via supabaseAdmin (never trusts client); `exportGateDecision` returns 402 for non-creator. Partial config (supabaseAdmin null) is fail-closed to "free" → 402 via the `if (supabaseAdmin)` guard. Verified by tests/export-gate.test.ts. |
| T-03-02 | Denial of Service | app/api/export/route.tsx | mitigate | Validate input length 8..1500, require result.recommendation + scenarios array, reject oversized/malformed body with 400; `maxDuration = 30` caps render time. |
| T-03-03 | Tampering | components/StoryCard.tsx | accept | Satori renders text as text (no HTML injection into the PNG); input/recommendation are trimmed. No executable surface in a rasterized image. |
| T-03-04 | Information Disclosure | app/api/export/route.tsx | accept | Payload is the user's own simulation POSTed from their session; no cross-user data read; no persistence. |
</threat_model>

<verification>
- `npm test` — export-gate + export-render specs green (EXPORT-03, EXPORT-01/02 contract).
- `npx tsc --noEmit` — no type errors across StoryCard, route, ShareCard, ResultView.
- `npm run build` — Next build compiles the .tsx route + client components.
- `git diff package.json` empty — no new npm dependency (next/og bundled).
- Demo-mode preserved: with NEXT_PUBLIC_SUPABASE_URL unset, the gate treats the viewer as creator and the route renders (mirrors lib/quota fail-open) — asserted by the export-gate demoMode case.
- Partial-config safety: NEXT_PUBLIC_SUPABASE_URL set but SUPABASE_SERVICE_ROLE_KEY unset (supabaseAdmin null) resolves plan "free" → 402, never a 500 null-deref.
- Manual (founder): POST a sample payload as a creator (or in demo mode), open the returned PNG, confirm 1080x1920, wordmark + question (>=64px) + recommendation + 3 tags/probabilities legible at TikTok overlay scale (EXPORT-02).
</verification>

<success_criteria>
- EXPORT-01: Creator downloads a 9:16 (1080x1920) PNG named whatif-simulation.png via next/og ImageResponse.
- EXPORT-02: Card branded (WhatIf wordmark + user's question + recommendation + 3 scenario tags/probabilities), question >=64px, no text <28px, readable at overlay scale.
- EXPORT-03: /api/export returns 402 + { error: "upgrade_required", upsell } for free/pro (and partial-config); 200 image/png for creator; demo mode allows.
- CONTENT-04: /result Share affordance links free/pro users to the Creator upgrade and gives Creators the download.
- No new dependencies; runtime nodejs; only existing globals.css tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-new-features/03-01-SUMMARY.md`
</output>
