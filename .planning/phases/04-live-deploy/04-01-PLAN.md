---
phase: 04-live-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/impressum/page.tsx
  - app/datenschutz/page.tsx
  - components/Footer.tsx
  - components/ResultView.tsx
  - app/account/page.tsx
  - .env.example
autonomous: true
requirements: [DEPLOY-04]
must_haves:
  truths:
    - "A visitor can open /impressum and read an Impressum with the business@what-if.tech contact"
    - "A visitor can open /datenschutz and read a DSGVO privacy policy naming the real processors (Supabase, Stripe, OpenAI, Resend/IONOS)"
    - "The Footer Privacy and Terms links navigate to real pages, not href='#'"
    - "A user at the paywall (result upsell + account free-plan block) sees the discreet German test-mode notice"
  artifacts:
    - path: "app/impressum/page.tsx"
      provides: "Impressum (§5 DDG) server component with metadata export"
      contains: "business@what-if.tech"
    - path: "app/datenschutz/page.tsx"
      provides: "Datenschutz (DSGVO) server component with metadata export"
      contains: "Supabase"
    - path: "components/Footer.tsx"
      provides: "Wired legal links"
      contains: "/impressum"
  key_links:
    - from: "components/Footer.tsx"
      to: "/impressum and /datenschutz"
      via: "next/link Link href"
      pattern: "href=\"/(impressum|datenschutz)\""
    - from: "components/ResultView.tsx upsell block"
      to: "test-mode notice"
      via: "static micro-copy line after UpgradeButton"
      pattern: "Testphase"
---

<objective>
Add the two public legal pages (`/impressum`, `/datenschutz`), wire the Footer's dead `href="#"` links to them, and place a discreet German test-mode notice on the two paywall surfaces (result upsell block + account free-plan block). This is the code prerequisite for going public — a German SaaS taking signups needs a reachable Impressum and Datenschutzerklärung, and the paywall must honestly signal that payments run in test mode this phase.

Purpose: WhatIf cannot ship publicly to a German audience without a linked Impressum + Datenschutz, and the paywall must not imply real charges while Stripe is in test mode (D-08, D-10).
Output: Two new server-component pages, one Footer edit, two paywall-surface edits, one documentation-only `.env.example` comment tweak.

Language decision (Claude's Discretion per D-09, hereby resolved): the legal pages are written in **German**. Impressum (§5 DDG) and Datenschutzerklärung (DSGVO) are German legal instruments addressed to German authorities/users; German is the legally correct and conventional language even though the product UI copy is English. The Footer link labels stay English ("Privacy"/"Terms") to match the surrounding footer, but the pages themselves are German.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-live-deploy/04-CONTEXT.md
@.planning/phases/04-live-deploy/04-PATTERNS.md

<interfaces>
<!-- Static-page shell to copy (server component, has metadata, NO "use client"). From app/account/page.tsx lines 1-13, 44-58. Strip Supabase/auth/redirect logic; keep Nav / BackgroundOrbs / max-w-2xl container / rounded-2xl card / Footer. -->

Imports available at project root (path alias @/* → project root):
  import Nav from "@/components/Nav";
  import Footer from "@/components/Footer";
  import BackgroundOrbs from "@/components/BackgroundOrbs";

Page metadata pattern:
  export const metadata = { title: "Impressum — WhatIf" };

Container + card shell (from app/account/page.tsx):
  <Nav />
  <section className="relative overflow-hidden">
    <BackgroundOrbs />
    <div className="mx-auto max-w-2xl px-6 pb-24 pt-10 md:pt-16">
      <h1 className="mb-8 font-display text-4xl leading-tight md:text-5xl">Impressum</h1>
      <div className="rounded-2xl border border-border-hi bg-surface/60 p-6 backdrop-blur-sm md:p-8">
        {/* prose: <h2 className="font-display text-2xl"> section headers, <p className="text-fg-soft leading-relaxed"> body */}
      </div>
    </div>
  </section>
  <Footer />

Discreet micro-copy idiom (from ResultView.tsx recurring label + UpgradeButton demo-note):
  <p className="mt-2 text-center text-[11px] text-fg-mute">
    Testphase · Zahlungen im Testmodus, keine echte Abbuchung
  </p>

Internal-link convention: use next/link <Link> for internal routes; keep <a> only for mailto.
  import Link from "next/link";
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /impressum and /datenschutz German legal pages</name>
  <files>app/impressum/page.tsx, app/datenschutz/page.tsx</files>
  <read_first>
    - app/account/page.tsx (the static-shell analog — copy the server-component structure, strip Supabase/auth/redirect)
    - .planning/phases/04-live-deploy/04-PATTERNS.md (shell excerpt + placeholder convention)
    - .planning/phases/04-live-deploy/04-CONTEXT.md (D-08, D-09 — required processor list and placeholder rules)
  </read_first>
  <action>
    Create two new server-component pages (NO "use client", NO data fetching) using the shell from the `<interfaces>` block: Nav + BackgroundOrbs + `max-w-2xl` container + `rounded-2xl border border-border-hi bg-surface/60 p-6 backdrop-blur-sm md:p-8` card + Footer. Content is in German.

    `app/impressum/page.tsx`:
    - `export const metadata = { title: "Impressum — WhatIf" };`
    - `<h1 ...>Impressum</h1>`
    - Section "Angaben gemäß § 5 DDG": controller name + postal address as CLEARLY-MARKED placeholders the founder fills in — use the exact visible strings `[NAME PLACEHOLDER — Gründer trägt ein]` and `[ADRESSE PLACEHOLDER — Straße, PLZ, Ort — Gründer trägt ein]`. Do NOT fabricate a real name or address.
    - Section "Kontakt": `E-Mail: business@what-if.tech` (render as `<a href="mailto:business@what-if.tech">`).
    - Section "Verantwortlich i.S.d. § 18 Abs. 2 MStV": same `[NAME PLACEHOLDER ...]` placeholder.
    - A short "Haftung für Inhalte / Links" boilerplate paragraph in German is fine.

    `app/datenschutz/page.tsx`:
    - `export const metadata = { title: "Datenschutz — WhatIf" };`
    - `<h1 ...>Datenschutzerklärung</h1>`
    - "Verantwortlicher": `[NAME PLACEHOLDER — Gründer trägt ein]`, `[ADRESSE PLACEHOLDER ...]`, contact `business@what-if.tech` (mailto link).
    - "Eingesetzte Dienste und Datenverarbeitung" — substantially complete, one subsection per real processor (per D-09):
      * Supabase (EU-Region): Authentifizierung (E-Mail/Passwort) und Datenbank; speichert Konto-E-Mail, Plan-Status und Nutzungszähler.
      * Stripe: Zahlungsabwicklung (Abonnement); Zahlungsdaten werden von Stripe verarbeitet, nicht bei uns gespeichert.
      * OpenAI: der eingegebene Entscheidungstext wird zur Generierung der Simulation an das Modell gesendet und NICHT dauerhaft in einem Nutzerprofil gespeichert.
      * Resend / IONOS: Versand der Authentifizierungs-E-Mails (Bestätigung, Passwort-Zurücksetzen).
      * Rate-Limit- und Missbrauchs-Logging: IP-Adresse / Nutzer-ID werden zur Begrenzung von Anfragen (Burst-Limit) und zum Protokollieren von Missbrauchsversuchen verarbeitet.
    - "Rechtsgrundlage" (Art. 6 DSGVO), "Ihre Rechte" (Auskunft, Berichtigung, Löschung, Widerspruch — Art. 15-21 DSGVO), and a "Kontakt für Datenschutzanfragen: business@what-if.tech" section in German.

    Style body copy with `<h2 className="font-display text-2xl">` section headers and `<p className="text-fg-soft leading-relaxed">` paragraphs inside the card. Use only existing globals.css tokens (no new tokens). Per CLAUDE.md: no comments unless WHY is non-obvious.
  </action>
  <verify>
    <automated>test -f app/impressum/page.tsx && test -f app/datenschutz/page.tsx && grep -q "business@what-if.tech" app/impressum/page.tsx && grep -q "business@what-if.tech" app/datenschutz/page.tsx && grep -q "Supabase" app/datenschutz/page.tsx && grep -q "OpenAI" app/datenschutz/page.tsx && grep -q "Stripe" app/datenschutz/page.tsx && grep -q "NAME PLACEHOLDER" app/impressum/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/impressum/page.tsx` exists, exports `metadata` with title `"Impressum — WhatIf"`, and contains the string `business@what-if.tech`.
    - `app/impressum/page.tsx` contains the visible placeholder string `[NAME PLACEHOLDER — Gründer trägt ein]` (no fabricated name/address).
    - `app/datenschutz/page.tsx` exists, exports `metadata` with title `"Datenschutz — WhatIf"`, and names all four processors: contains `Supabase`, `Stripe`, `OpenAI`, and `Resend` (or `IONOS`).
    - Neither page contains `"use client"`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Both German legal pages render via the account-page shell, name the real processors, use founder-fill placeholders (not fabricated PII), and reach business@what-if.tech.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Footer links + add test-mode notice to both paywall surfaces + .env.example comment</name>
  <files>components/Footer.tsx, components/ResultView.tsx, app/account/page.tsx, .env.example</files>
  <read_first>
    - components/Footer.tsx (17 lines — only lines 10-11 change)
    - components/ResultView.tsx (upsell block lines 175-200 — insertion point)
    - app/account/page.tsx (free-plan block lines 77-83 — second insertion point)
    - .env.example (11-line env contract)
    - .planning/phases/04-live-deploy/04-PATTERNS.md (D-10 notice wording + insertion excerpts; Footer swap detail)
  </read_first>
  <action>
    1. `components/Footer.tsx`: add `import Link from "next/link";` at the top. Replace the two dead `<a href="#" className="transition-colors hover:text-fg">Privacy</a>` and `<a href="#" ...>Terms</a>` lines (lines 10-11) with next/link routes — Privacy → `/datenschutz`, Terms → `/impressum` (DE convention: Privacy=Datenschutz, Impressum is the closest match for "Terms" until AGB ships):
       `<Link href="/datenschutz" className="transition-colors hover:text-fg">Privacy</Link>`
       `<Link href="/impressum" className="transition-colors hover:text-fg">Terms</Link>`
       Leave the Contact `<a href="mailto:business@what-if.tech">` line UNCHANGED (already correct per commit cadd82c — verify only). Do NOT touch Faq.tsx or StoryCard.tsx (email/domain swaps already done in cadd82c — verification-only, no edit).

    2. `components/ResultView.tsx`: inside the `showUpsell` upsell block, immediately after the `<div className="mt-4 flex flex-col gap-2 ...">` UpgradeButton/Compare-plans row (closes at line ~198), add the discreet test-mode notice line:
       `<p className="mt-2 text-center text-[11px] text-fg-mute">Testphase · Zahlungen im Testmodus, keine echte Abbuchung</p>`

    3. `app/account/page.tsx`: inside the non-subscriber (free-plan) block (`<div className="space-y-3">`, lines 77-83), after the two `<UpgradeButton>` lines, append the same notice:
       `<p className="mt-2 text-center text-[11px] text-fg-mute">Testphase · Zahlungen im Testmodus, keine echte Abbuchung</p>`

    4. `.env.example`: documentation-only comment tweak (no functional change) to clarify the test-vs-live distinction for future maintainers. Replace the current line 4 comment `# Stripe — leave empty to use the built-in demo checkout` with two lines:
       `# Stripe — leave empty to use the built-in demo checkout.`
       `# sk_test_... = real Stripe test-mode flow (NOT demo). sk_live_... = real money.`
       Do NOT change the `NEXT_PUBLIC_URL` value in .env.example — it stays `http://localhost:3000` (that file documents local dev; production value goes in Vercel env, handled in Plan 02).
  </action>
  <verify>
    <automated>grep -q 'href="/impressum"' components/Footer.tsx && grep -q 'href="/datenschutz"' components/Footer.tsx && ! grep -q 'href="#"' components/Footer.tsx && grep -c "Testphase · Zahlungen im Testmodus, keine echte Abbuchung" components/ResultView.tsx && grep -c "Testphase · Zahlungen im Testmodus, keine echte Abbuchung" app/account/page.tsx && grep -q "sk_test_" .env.example && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `components/Footer.tsx` contains `href="/impressum"` and `href="/datenschutz"` and has NO remaining `href="#"`.
    - `components/Footer.tsx` imports `Link` from `next/link`; the Contact mailto line is unchanged (`mailto:business@what-if.tech`).
    - `components/ResultView.tsx` contains exactly one occurrence of the string `Testphase · Zahlungen im Testmodus, keine echte Abbuchung`, positioned inside the `showUpsell` block.
    - `app/account/page.tsx` contains exactly one occurrence of the same notice string, inside the free-plan (non-subscriber) block.
    - `.env.example` contains the clarifying comment line mentioning `sk_test_` and `sk_live_`; the `NEXT_PUBLIC_URL=http://localhost:3000` line is unchanged.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Footer legal links resolve to the new pages, the test-mode notice appears on both Unlock surfaces, and .env.example documents the test-vs-live key distinction — all with tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public visitor → legal pages | Untrusted, unauthenticated visitors read /impressum + /datenschutz |
| build-time env → rendered page | .env.example / page source must not embed secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | app/impressum/page.tsx, app/datenschutz/page.tsx | mitigate | Pages are static server components with hard-coded German prose only — no `process.env` reads, no secrets, no PII beyond the founder-filled placeholder markers. Acceptance criterion greps confirm only `business@what-if.tech` + placeholder tokens appear. |
| T-04-02 | Information Disclosure | .env.example | mitigate | Edit is comment-only; the file stays a template with EMPTY values (no real keys). Verify grep confirms no `sk_live_`/`sk_test_` *value* is added, only the doc comment mentioning the prefixes. |
| T-04-03 | Tampering | components/Footer.tsx links | accept | Internal `next/link` routes to `/impressum` + `/datenschutz` — no user-controlled href, no open-redirect surface. Low risk. |
| T-04-04 | Information Disclosure | ResultView.tsx / account test-mode notice | mitigate | Notice is a fixed German string; it deliberately does NOT render any key, plan-secret, or Stripe identifier. Acceptance criterion pins the exact literal, preventing accidental interpolation of sensitive data. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run build` succeeds (both new routes compile as static pages).
- Manual (folded into Plan 03 smoke test): /impressum and /datenschutz reachable and readable; Footer Privacy/Terms links navigate; test-mode notice visible at the paywall.
</verification>

<success_criteria>
- Two German legal pages exist and are reachable, naming the real processors and the business@what-if.tech contact, with clearly-marked founder placeholders for name/address.
- Footer has zero `href="#"` links; Privacy→/datenschutz, Terms→/impressum.
- The discreet test-mode notice appears on both the ResultView upsell block and the account free-plan block.
- tsc + build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-deploy/04-01-SUMMARY.md`
</output>
