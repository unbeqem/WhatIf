---
phase: 04-live-deploy
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified: []
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]
user_setup:
  - service: openai
    why: "Real simulations in production (demo OFF) — DEPLOY-01"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard → API keys (must have billing enabled)"
  - service: supabase
    why: "Production auth + DB + plan reads (EU region, existing project) — DEPLOY-03"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase project zdirwmqfoynxmfifzlvt → Settings → API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase → Settings → API → anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase → Settings → API → service_role key (SERVER-ONLY, never client-exposed)"
    dashboard_config:
      - task: "Set Auth Site URL to https://what-if.tech and add it (plus https://what-if.tech/auth/confirm) to the redirect allowlist"
        location: "Supabase → Authentication → URL Configuration"
      - task: "Confirm Resend/IONOS custom SMTP sender + email template links resolve to what-if.tech"
        location: "Supabase → Authentication → Email templates / SMTP settings"
  - service: stripe
    why: "Test-mode checkout + webhook plan-flip in production — DEPLOY-02 (test mode this phase)"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe dashboard (TEST mode) → Developers → API keys → sk_test_..."
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe dashboard (TEST mode) → Developers → Webhooks → the what-if.tech endpoint → Signing secret (whsec_...)"
    dashboard_config:
      - task: "Register TEST-mode webhook endpoint https://what-if.tech/api/stripe/webhook subscribed to checkout.session.completed, customer.subscription.updated, customer.subscription.deleted"
        location: "Stripe dashboard (TEST mode) → Developers → Webhooks → Add endpoint"
  - service: vercel
    why: "Host the production build with the env matrix + NEXT_PUBLIC_URL"
    env_vars:
      - name: NEXT_PUBLIC_URL
        source: "Set to https://what-if.tech (NO trailing slash — code concatenates ${baseUrl}/path)"
      - name: ANON_COOKIE_SECRET
        source: "Reuse the existing 32-byte hex secret (openssl rand -hex 32) — must match the value used to sign anon cookies"
must_haves:
  truths:
    - "The Vercel production project has all env vars from .env.example set with production values"
    - "OpenAI runs live in prod (real simulations, demo OFF)"
    - "Supabase runs live EU in prod (real auth + plan reads, demo OFF)"
    - "Stripe runs in TEST mode in prod (test keys present → stripeIsLive true, NOT demo fallback)"
    - "The Stripe test-mode webhook endpoint is registered against https://what-if.tech/api/stripe/webhook"
    - "Supabase Auth Site URL + redirect allowlist include https://what-if.tech so /auth/confirm lands"
  artifacts:
    - path: ".env.example"
      provides: "The env contract the Vercel matrix must mirror (finalized in Plan 01)"
      contains: "NEXT_PUBLIC_URL"
  key_links:
    - from: "Vercel env NEXT_PUBLIC_URL=https://what-if.tech"
      to: "lib/stripe.ts + auth redirect base"
      via: "process.env.NEXT_PUBLIC_URL read at all 5 call sites (no code change)"
      pattern: "NEXT_PUBLIC_URL"
    - from: "Stripe test webhook endpoint"
      to: "app/api/stripe/webhook/route.ts"
      via: "signed POST with whsec_ test secret"
      pattern: "what-if.tech/api/stripe/webhook"
---

<objective>
Configure the Vercel production environment and the three external dashboards (OpenAI billing key, EU Supabase URLs, Stripe test-mode webhook) so that the already-live https://what-if.tech deployment runs on real OpenAI + real EU Supabase with demo OFF, and Stripe in TEST mode (test keys, NOT the demo fallback). No source code changes — this is pure env/dashboard configuration, verified against the finalized `.env.example` contract from Plan 01.

Purpose: "Demo OFF for OpenAI/Supabase, test-mode (not demo) for Stripe" is achieved entirely by which env vars are non-empty in Vercel (D-06, D-07) plus three dashboard registrations. This plan supplies the keys and registers the endpoints.
Output: A fully env-configured Vercel project + Supabase Auth URLs + Stripe test webhook — all observable/tickable, no code diff.

Why autonomous: false — every step is founder dashboard work (Vercel env entry, Supabase URL config, Stripe webhook registration). An agent cannot click these dashboards; the founder ticks the acceptance criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-live-deploy/04-CONTEXT.md
@.planning/phases/04-live-deploy/04-PATTERNS.md
@.env.example

<interfaces>
<!-- The env contract (from .env.example, finalized in Plan 01). The Vercel project must set production values for every one of these. -->

OPENAI_API_KEY            → real key with billing (demo OFF)                — DEPLOY-01
STRIPE_SECRET_KEY         → sk_test_...  (test mode, NOT demo)              — DEPLOY-02
STRIPE_WEBHOOK_SECRET     → whsec_...    (from the what-if.tech endpoint)   — DEPLOY-02
NEXT_PUBLIC_URL           → https://what-if.tech  (NO trailing slash)        — D-02
NEXT_PUBLIC_SUPABASE_URL  → EU project zdirwmqfoynxmfifzlvt URL              — DEPLOY-03
NEXT_PUBLIC_SUPABASE_ANON_KEY → anon public key                             — DEPLOY-03
SUPABASE_SERVICE_ROLE_KEY → service_role key (SERVER-ONLY)                   — DEPLOY-03
ANON_COOKIE_SECRET        → existing 32-byte hex (must match cookie signer)

<!-- Guard logic (NO code change — from lib/openai.ts / lib/stripe.ts): key presence flips isLive/stripeIsLive true. Empty key → demo fallback. -->
lib/openai.ts:  const client = apiKey ? new OpenAI({ apiKey }) : null; export const isLive = Boolean(client);
lib/stripe.ts:  export const stripe = secret ? new Stripe(secret, {...}) : null; export const stripeIsLive = Boolean(stripe);

<!-- The 5 functional NEXT_PUBLIC_URL call sites that setting the Vercel value covers (no edits): -->
  lib/stripe.ts:31 (checkout success/cancel), lib/stripe.ts:95 (portal return),
  app/api/stripe/portal/route.ts:9, app/auth/signup/route.ts:19-21 (emailRedirectTo),
  app/auth/reset-request/route.ts:19 (reset redirectTo)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set the Vercel production env matrix</name>
  <read_first>
    - .env.example (the exact var names + comments to mirror)
    - .planning/phases/04-live-deploy/04-PATTERNS.md (env matrix table + guard-by-presence explanation)
    - .planning/phases/04-live-deploy/04-CONTEXT.md (D-02, D-03, D-06, D-07)
  </read_first>
  <what-built>Plan 01 finalized `.env.example` as the env contract. This step mirrors it into the Vercel project with production values so demo is OFF for OpenAI + Supabase and Stripe runs test-mode.</what-built>
  <action>
    In the Vercel project (Settings → Environment Variables, Production scope), set every var from `.env.example` with these production values:
    - `OPENAI_API_KEY` = your real OpenAI key (billing enabled) — turns demo OFF for simulations.
    - `STRIPE_SECRET_KEY` = your `sk_test_...` key — test mode, NOT demo (presence flips stripeIsLive true).
    - `STRIPE_WEBHOOK_SECRET` = the `whsec_...` from the what-if.tech endpoint you register in Task 3 (set/update after Task 3).
    - `NEXT_PUBLIC_URL` = `https://what-if.tech` — NO trailing slash (code does `${baseUrl}/path`).
    - `NEXT_PUBLIC_SUPABASE_URL` = the EU project zdirwmqfoynxmfifzlvt URL.
    - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = the anon public key.
    - `SUPABASE_SERVICE_ROLE_KEY` = the service_role key — SERVER-ONLY, never expose to client, never commit.
    - `ANON_COOKIE_SECRET` = the existing 32-byte hex secret (must match whatever signed existing anon cookies, else anon quota resets).
    Redeploy so the env takes effect.
  </action>
  <acceptance_criteria>
    - Every var name in `.env.example` (8 vars) exists in the Vercel Production env with a non-empty value.
    - `NEXT_PUBLIC_URL` is exactly `https://what-if.tech` with NO trailing slash.
    - `STRIPE_SECRET_KEY` begins with `sk_test_` (test mode, not `sk_live_`, not empty).
    - `OPENAI_API_KEY` and the three Supabase vars are non-empty (demo OFF for both services).
    - `SUPABASE_SERVICE_ROLE_KEY` is set only in the server/Production scope (no `NEXT_PUBLIC_` prefix — never client-exposed).
    - After redeploy, https://what-if.tech loads without the demo banner and a simulation returns real OpenAI output (confirmed in Plan 03 smoke).
  </acceptance_criteria>
  <resume-signal>Type "env set" once all 8 vars are configured in Vercel Production and the project has redeployed.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Configure Supabase Auth Site URL + redirect allowlist + confirm SMTP sender</name>
  <read_first>
    - .planning/phases/04-live-deploy/04-CONTEXT.md (Integration Points — Supabase Auth Site URL must include the domain for /auth/confirm to land)
    - app/auth/signup/route.ts and app/auth/reset-request/route.ts (emailRedirectTo / redirectTo use NEXT_PUBLIC_URL → /auth/confirm)
    - STATE.md (Resend custom SMTP already configured; email templates use {{ .TokenHash }} OTP links)
  </read_first>
  <what-built>Phase 1 wired email auth to use NEXT_PUBLIC_URL as the redirect base and OTP token_hash confirm links. This step points Supabase's own Site URL / redirect allowlist at what-if.tech so confirmation + reset emails land on the production domain.</what-built>
  <action>
    In Supabase (project zdirwmqfoynxmfifzlvt) → Authentication → URL Configuration:
    - Set Site URL to `https://what-if.tech`.
    - Add `https://what-if.tech` and `https://what-if.tech/auth/confirm` (and `https://what-if.tech/auth/confirm?next=/reset` if the allowlist is path-specific) to the Redirect URLs allowlist. Keep the allowlist tight — only what-if.tech origins (no wildcard, to avoid open-redirect on auth callback).
    - In Authentication → Email templates / SMTP: confirm the Resend/IONOS custom SMTP sender is active and that the confirmation + reset templates use `{{ .TokenHash }}` links resolving to the what-if.tech origin (per STATE.md, already switched — verify only).
    - (Non-blocking, founder-side per D-04) Optionally delete the dev test user 8e36b3a9-8ad3-43f0-8df3-4aa481a3574a so prod starts clean.
  </action>
  <acceptance_criteria>
    - Supabase Auth Site URL is exactly `https://what-if.tech`.
    - The redirect allowlist contains `https://what-if.tech/auth/confirm` and no overly-broad wildcard entry.
    - A signup confirmation email link (verified in Plan 03) points at `https://what-if.tech/auth/confirm?...token_hash=...` and lands successfully.
    - SMTP sender is the Resend/IONOS custom sender (not the Supabase 3-emails/h default).
  </acceptance_criteria>
  <resume-signal>Type "supabase urls set" once Site URL + redirect allowlist point at what-if.tech and SMTP sender is confirmed.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Register the Stripe TEST-mode webhook endpoint + confirm test products/prices</name>
  <read_first>
    - app/api/stripe/webhook/route.ts (raw-body signature verify — needs the whsec_ from THIS endpoint)
    - .planning/phases/04-live-deploy/04-CONTEXT.md (D-05 — register Vercel URL as test-mode webhook; test products Pro €5 / Creator €9)
    - lib/stripe.ts (plan config; checkout uses the test prices)
  </read_first>
  <what-built>Phase 2 built the webhook handler that verifies a Stripe signature and flips the DB plan. Local dev used `stripe listen`. This step registers a persistent TEST-mode endpoint at the production URL so plan flips work in prod with test events.</what-built>
  <action>
    In the Stripe dashboard, TEST mode (toggle top-right must show "Test mode"):
    - Developers → Webhooks → Add endpoint: URL `https://what-if.tech/api/stripe/webhook`, subscribe to the three events the handler processes: `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`.
    - Copy the endpoint's Signing secret (`whsec_...`) and set it as `STRIPE_WEBHOOK_SECRET` in Vercel (updates Task 1's placeholder), then redeploy.
    - Confirm test-mode products/prices exist for Pro (€5/mo) and Creator (€9/mo) matching lib/stripe.ts plan config.
    - Confirm the Customer Portal is configured/saved in TEST mode (PAY-06 prerequisite — per STATE.md already saved; verify).
  </action>
  <acceptance_criteria>
    - A TEST-mode webhook endpoint `https://what-if.tech/api/stripe/webhook` exists subscribed to exactly `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`.
    - `STRIPE_WEBHOOK_SECRET` in Vercel equals this endpoint's signing secret (starts with `whsec_`), and the project was redeployed after setting it.
    - Test-mode Pro (€5) and Creator (€9) products/prices exist.
    - Customer Portal is saved in TEST mode.
    - A test event delivered to the endpoint returns HTTP 2xx (signature verified) — confirmed in Plan 03 smoke.
  </acceptance_criteria>
  <resume-signal>Type "stripe webhook registered" once the test-mode endpoint exists, whsec_ is in Vercel, and the project redeployed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel env store → runtime | Production secrets (service-role, Stripe, OpenAI keys) live server-side only |
| Stripe → webhook endpoint | Untrusted POST crosses into app/api/stripe/webhook; must be signature-verified |
| email link → /auth/confirm | Auth callback redirect target must be allowlisted (open-redirect surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY, OPENAI_API_KEY, STRIPE_SECRET_KEY | mitigate | Set only in Vercel server/Production scope, never with a `NEXT_PUBLIC_` prefix, never committed. Acceptance criterion pins service-role to server-only scope. |
| T-04-06 | Spoofing | app/api/stripe/webhook/route.ts | mitigate | Endpoint must be registered with the correct `whsec_` test secret so the existing raw-body signature verification (Phase 2) accepts only genuine Stripe deliveries. Criterion requires the whsec_ in Vercel to match the registered endpoint. |
| T-04-07 | Tampering (open redirect) | Supabase redirect allowlist / /auth/confirm | mitigate | Allowlist restricted to https://what-if.tech origins only, no wildcard — prevents auth-callback redirect abuse. |
| T-04-08 | Elevation of Privilege | Stripe TEST vs demo mode | accept | Test keys present → real (test) Stripe code path, no real money moves this phase (intentional re-scope, D-05/D-06). Live keys deferred post-Gewerbe. |
| T-04-09 | Information Disclosure | demo-mode left on in prod | mitigate | Supplying non-empty OPENAI + Supabase keys flips demo OFF (guard-by-presence). Plan 03 smoke confirms no demo banner and real OpenAI output. |
</threat_model>

<verification>
- All 8 env vars present in Vercel Production (mirror of .env.example).
- Stripe test webhook endpoint registered at what-if.tech/api/stripe/webhook with the 3 events; whsec_ in Vercel.
- Supabase Site URL + redirect allowlist = what-if.tech.
- Full functional proof happens in Plan 03 (live E2E smoke).
</verification>

<success_criteria>
- Production runs live OpenAI + live EU Supabase (demo OFF), Stripe in test mode (not demo). [DEPLOY-01, DEPLOY-03]
- Stripe test webhook endpoint registered against the production URL with its whsec_ wired into Vercel. [DEPLOY-02]
- Supabase auth redirects land on what-if.tech.
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-deploy/04-02-SUMMARY.md`
</output>
