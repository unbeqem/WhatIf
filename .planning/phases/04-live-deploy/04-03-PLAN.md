---
phase: 04-live-deploy
plan: 03
type: execute
wave: 3
depends_on: [1, 2]
files_modified: []
autonomous: false
requirements: [DEPLOY-02, DEPLOY-04]
must_haves:
  truths:
    - "The founder can sign up on https://what-if.tech, receive a confirmation email, and land on /auth/confirm successfully"
    - "A real OpenAI simulation returns on the live domain within 8 seconds"
    - "The soft paywall shows the discreet test-mode notice"
    - "A Stripe TEST-card checkout completes and the webhook flips the user's plan to pro/creator in the production DB within seconds"
    - "A Creator user can download the 9:16 story-card PNG on the live domain"
    - "The user can open the Stripe Customer Portal and cancel from /account"
    - "/impressum and /datenschutz are reachable from the Footer on the live domain"
  artifacts:
    - path: ".planning/phases/04-live-deploy/04-03-SUMMARY.md"
      provides: "Founder-signed launch-gate smoke result"
      contains: "DEPLOY-04"
  key_links:
    - from: "https://what-if.tech test-card checkout"
      to: "production profiles.plan"
      via: "Stripe test webhook → app/api/stripe/webhook"
      pattern: "plan.*(pro|creator)"
    - from: "confirmation email link"
      to: "https://what-if.tech/auth/confirm"
      via: "Supabase OTP token_hash redirect"
      pattern: "what-if.tech/auth/confirm"
---

<objective>
Founder end-to-end smoke test on the live https://what-if.tech domain using Stripe TEST cards: the full funnel — signup → confirm email → simulate (real OpenAI, <8s) → hit paywall (with test-mode notice) → test-card pay → plan flips via webhook in the production DB → Creator story-card export → manage/cancel in the Customer Portal. Also verifies the two new legal pages are reachable and eyeballs the story-card PNG at TikTok overlay scale (the deferred EXPORT-02 founder check).

Purpose: This is the launch gate. DEPLOY-04 (deployed on domain + working email auth + successful simulation flow) and the test-mode half of DEPLOY-02 (webhook plan-flip observed with a test event) are proven here. Live-money DEPLOY-02/DEPLOY-04 remain intentionally deferred post-Gewerbe (out of scope — see CONTEXT.md).
Output: A completed founder checklist recorded in the SUMMARY confirming the machine works end-to-end in production with test cards.

Why autonomous: false — this is a human running the real flow through a browser + email inbox + Stripe test card on the live URL. An agent cannot receive the confirmation email, enter a test card, or eyeball the PNG.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-live-deploy/04-CONTEXT.md
@.planning/phases/04-live-deploy/04-PATTERNS.md

<interfaces>
<!-- Live flow surfaces (all already built in Phases 1-3; this plan exercises them in prod): -->
  /            → landing (Footer: Privacy→/datenschutz, Terms→/impressum, Contact mailto)
  /login,/signup → email/password auth (Supabase, OTP token_hash confirm)
  /auth/confirm  → email-link landing (needs Supabase Site URL = what-if.tech)
  /decision    → SimulateForm → POST /api/simulate (real OpenAI when key present)
  /result      → ResultView (soft-paywall upsell + test-mode notice + ShareCard/story-card export)
  /account     → plan badge + UpgradeButtons (+ test-mode notice) + Manage Subscription (Customer Portal)
  /api/stripe → checkout (test mode); /api/stripe/webhook → plan flip; /api/stripe/portal → portal

<!-- Stripe test card: 4242 4242 4242 4242, any future expiry, any CVC, any postal code. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live funnel smoke — signup → confirm → simulate → paywall → test-pay → plan flip</name>
  <read_first>
    - .planning/phases/04-live-deploy/04-CONTEXT.md (in-scope flow + test-mode boundary)
    - STATE.md (Phase 1-2 verify checklist — the same steps, now on the live domain)
    - app/api/stripe/webhook/route.ts (the 3 events that flip the plan)
  </read_first>
  <what-built>Phases 1-3 built the entire funnel; Plan 01 added legal pages + test-mode notice; Plan 02 configured prod env + Supabase URLs + the Stripe test webhook. This step runs the whole loop live with a test card.</what-built>
  <action>
    Founder-manual verification (no code). On https://what-if.tech in a clean browser session, run the full funnel with a Stripe TEST card and observe the plan flip in the production Supabase, per the numbered steps in how-to-verify. Record each result in 04-03-SUMMARY.md.
  </action>
  <how-to-verify>
    On https://what-if.tech in a clean browser session:
    1. Sign up with a real inbox (email + password). Expect a confirmation email from the Resend/IONOS sender.
    2. Click the confirmation link — it MUST land on `https://what-if.tech/auth/confirm` and log you in (not localhost, not a *.vercel.app URL).
    3. Go to /decision, submit a real decision. The result MUST be a real OpenAI simulation (not the canned demo), returned within 8 seconds.
    4. Trigger the paywall (free-tier second attempt, or view the /result upsell / /account free-plan block). Confirm the discreet notice reads "Testphase · Zahlungen im Testmodus, keine echte Abbuchung".
    5. Click Unlock Pro (or Go Creator). In Stripe Checkout, pay with test card 4242 4242 4242 4242 (any future expiry, any CVC).
    6. Return to the app. Within seconds, /account MUST show plan = Pro (or Creator). Confirm the webhook fired: in Supabase, the profiles row for your user shows plan='pro' (or 'creator').
  </how-to-verify>
  <verify>
    <automated>MISSING — inherently manual live-domain E2E (browser + email inbox + Stripe test card). Founder ticks the acceptance criteria below.</automated>
  </verify>
  <acceptance_criteria>
    - Confirmation email link resolves to `https://what-if.tech/auth/confirm` and successfully authenticates.
    - A /decision submission returns a real (non-demo) OpenAI result in ≤ 8 seconds.
    - The paywall surface shows the exact string "Testphase · Zahlungen im Testmodus, keine echte Abbuchung".
    - After test-card checkout, the production Supabase `profiles.plan` for the test user flips to `pro` or `creator` within seconds (webhook observed).
  </acceptance_criteria>
  <done>Founder confirms the full live funnel (signup→confirm→simulate<8s→paywall→test-pay→plan flip) works on what-if.tech, results recorded in SUMMARY.</done>
  <resume-signal>Type "funnel ok" (or describe the first step that failed).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Creator export + PNG eyeball + portal cancel + legal pages reachable</name>
  <read_first>
    - components/StoryCard.tsx (9:16 export, what-if.tech footer text — cosmetic swap already committed)
    - app/account/ManageSubscriptionButton (Customer Portal entry)
    - components/Footer.tsx (Privacy/Terms links wired in Plan 01)
  </read_first>
  <what-built>The Creator 9:16 story-card export (Phase 3), the Customer Portal round-trip (Phase 2), and the two new legal pages + Footer links (Plan 01) — all exercised live.</what-built>
  <action>
    Founder-manual verification (no code). As the now-Creator/Pro user, download and eyeball the 9:16 story-card PNG at TikTok scale (closes deferred EXPORT-02), round-trip the Stripe Customer Portal cancel, and confirm the Footer legal links reach /datenschutz + /impressum on the live domain, per how-to-verify. Record results in 04-03-SUMMARY.md.
  </action>
  <how-to-verify>
    Continuing on https://what-if.tech (as the now-Creator/Pro user from Task 1):
    1. On /result as a Creator-tier user, click "Download story card". A 9:16 PNG downloads, branded with the WhatIf logo and your original question, footer reads "what-if.tech — simulate your decision".
    2. EXPORT-02 eyeball: open the PNG and confirm the question + branding are legible at TikTok overlay scale (roughly 1080×1920, viewed small). This closes the deferred 03-HUMAN-UAT.md PNG check.
    3. On /account, click Manage subscription → Stripe Customer Portal opens (test mode). Cancel the subscription. Confirm the app remains under the WhatIf brand context (returns to /account).
    4. In the Footer (any page), click Privacy → lands on /datenschutz; click Terms → lands on /impressum. Both pages render the German legal content with the business@what-if.tech contact.
  </how-to-verify>
  <verify>
    <automated>MISSING — inherently manual live-domain checks (PNG eyeball, portal round-trip, live-link navigation). Founder ticks the acceptance criteria below.</automated>
  </verify>
  <acceptance_criteria>
    - A 9:16 PNG downloads on the live domain, legible at TikTok overlay scale (EXPORT-02 founder confirmation recorded).
    - The Customer Portal opens from /account in test mode and a cancel completes, returning to /account.
    - Footer Privacy → /datenschutz and Terms → /impressum both load the German legal pages on the live domain; both show business@what-if.tech.
  </acceptance_criteria>
  <done>Founder confirms Creator export (legible PNG), portal cancel, and both legal pages reachable from the Footer on what-if.tech.</done>
  <resume-signal>Type "launch gate passed" (or describe any failure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public browser → live app | Real users hit the production funnel; demo must be OFF, secrets must not leak |
| Stripe test event → prod DB | Webhook must verify signature before flipping a plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure | live page source / network | mitigate | Smoke step confirms no demo banner and that no secret (service-role, sk_, whsec_) appears in client bundle or network responses; real OpenAI output proves live keys are server-side only. |
| T-04-11 | Spoofing | production webhook | mitigate | Plan-flip only occurs via a signature-verified Stripe test event (endpoint whsec_ from Plan 02); observing the flip proves verification path is intact in prod. |
| T-04-12 | Repudiation | launch-gate result | mitigate | Founder records the pass/fail per acceptance criterion in 04-03-SUMMARY.md — an auditable launch-gate record. |
| T-04-13 | Elevation of Privilege | test vs live money | accept | All payments are Stripe test cards this phase; no real charge (intentional re-scope). Live-money smoke deferred post-Gewerbe. |
</threat_model>

<verification>
- Full live funnel passes with a Stripe test card; plan flip observed in production Supabase.
- Story-card PNG legible at TikTok scale (EXPORT-02 closed).
- Portal cancel works; legal pages reachable from Footer.
- All results recorded in 04-03-SUMMARY.md.
</verification>

<success_criteria>
- Founder completes signup → confirm → simulate (<8s) → paywall → test-card pay → plan flip → export → portal cancel on the live domain. [DEPLOY-04, test-mode]
- Webhook plan-flip observed in prod with a Stripe test event. [DEPLOY-02, test-mode]
- Legal pages reachable; test-mode notice visible.
- Live-money payment + live Stripe keys remain intentionally deferred (out of scope this phase).
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-deploy/04-03-SUMMARY.md`
</output>
